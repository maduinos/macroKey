# macroKey 아키텍처

버튼 8개짜리 Pro Micro 키패드를 "고정 단축키 8개"에서 "확장 가능한 매크로 플랫폼"으로
바꾸기 위한 설계 문서입니다. 하드웨어 배선은 [`HARDWARE.md`](HARDWARE.md), 호스트와 장치가
주고받는 문자열 규격은 [`PROTOCOL.md`](PROTOCOL.md)에 따로 정리했습니다.

## 1. 설계 원칙

1. **Device-first, host-optional** — 펌웨어만 올려도 평범한 USB HID 키보드/마우스로 동작합니다.
   호스트 앱은 없어도 되는 증폭기이지 필수 런타임이 아닙니다. PC를 바꿔도, 앱을 안 깔아도
   키패드는 기본 기능을 합니다.
2. **정책과 메커니즘 분리** — 펌웨어는 "무엇을 눌렀나"를 이벤트로 만들고, "무엇을 할까"는
   프로필 테이블이 정합니다. 새 매크로를 추가할 때 펌웨어를 다시 빌드하지 않습니다.
3. **확장은 코드 수정이 아니라 등록으로** — 액션 타입은 테이블 항목 추가, 호스트 액션은
   registry 등록, LED 효과는 함수 포인터 추가로 늘어납니다. `if` 사슬을 늘리지 않습니다.
4. **모든 경계에 버전을 박는다** — 시리얼 프로토콜, EEPROM 프로필, 호스트 설정 JSON 각각
   독립 버전을 갖고, 상위 버전을 만나면 조용히 오동작하는 대신 거부하거나 마이그레이션합니다.
5. **바퀴를 다시 만들지 않는다** — LED에 실을 상태는 AgentPet
   [이벤트 프로토콜 v1](https://github.com/maduinos/AgentPet/blob/main/docs/EVENT_PROTOCOL.md)의
   `state`/`severity`/`progress`를 그대로 씁니다. macroKey 전용 상태 enum을 새로 만들지 않습니다.

## 2. 계층 구조

```
┌──────────────────────────────────────────────────────────────┐
│ L5  이벤트 소스     AgentPet · CI · 사용자 스크립트          │
│                     (ActivityEvent JSON, protocol v1)         │
└───────────────────────────┬──────────────────────────────────┘
                            │ Unix socket / TCP loopback
┌───────────────────────────▼──────────────────────────────────┐
│ L4  호스트 앱 (Python, macrokey 패키지)                       │
│     profile store │ action registry │ recorder │ led service  │
└───────────────────────────┬──────────────────────────────────┘
                            │ USB CDC 시리얼, protocol v1 (PROTOCOL.md)
┌───────────────────────────▼──────────────────────────────────┐
│ L3  펌웨어 코어                                               │
│     KeyEngine · ProfileStore(EEPROM) · LedController          │
└───────────────────────────┬──────────────────────────────────┘
┌───────────────────────────▼──────────────────────────────────┐
│ L2  HAL   ButtonInput(디바운스·제스처) · HID · WS2812B        │
└───────────────────────────┬──────────────────────────────────┘
┌───────────────────────────▼──────────────────────────────────┐
│ L1  하드웨어   버튼 8 · WS2812B 1 · ATmega32u4 native USB     │
└──────────────────────────────────────────────────────────────┘
```

각 계층은 아래 계층만 압니다. `KeyEngine`은 시리얼을 모르고, `LedController`는 버튼을 모르며,
호스트 앱은 어떤 핀에 무엇이 붙었는지 모릅니다.

## 3. 8개 버튼을 96개 슬롯으로 — 입력 모델

물리 키가 8개뿐이라는 제약을 **제스처 × 레이어**로 넘습니다.

### 3.1 제스처

`ButtonInput`이 원시 핀 상태를 다음 이벤트로 정규화합니다.

| 제스처 | 발화 시점 | 용도 |
| --- | --- | --- |
| `TAP` | 짧게 눌렀다 뗐을 때 | 기본 단축키 |
| `DOUBLE` | 임계 시간(기본 250 ms) 안에 두 번 탭 | 파괴적/드문 동작 |
| `HOLD` | 임계 시간(기본 400 ms) 넘게 유지 | 레이어 전환, modifier, 반복 |

`HOLD` 슬롯에 담긴 액션이 레이어 계열이면 **누르고 있는 동안만** 유효한 momentary 레이어로,
그 외에는 임계 시간에 한 번 발화(옵션으로 자동 반복)합니다. 하나의 슬롯이 두 의미를 겸하므로
제스처 종류를 늘리지 않고도 modifier 키패드처럼 쓸 수 있습니다.

`TAP`과 `DOUBLE`의 충돌은 흔한 함정입니다. 해당 키의 `DOUBLE` 슬롯이 비어 있으면 `TAP`을
릴리스 즉시 내보내고, 채워져 있을 때만 double 임계 시간만큼 지연시킵니다. 즉 **비용은 그
기능을 실제로 쓰는 키만 냅니다.**

### 3.2 레이어

레이어 4개 × 키 8개 × 제스처 3개 = **96 슬롯**. 레이어는 세 가지로 바뀝니다.

- momentary: 특정 키를 누르고 있는 동안 (예: 8번 키 HOLD)
- toggle: 탭으로 고정 전환
- host-driven: 호스트 앱이 활성 창을 보고 `LAYER set=` 을 내려보냄 (앱별 프로필)

레이어가 0이 아니면 LED가 해당 레이어 색으로 물들어 지금 어느 레이어인지 눈으로 보입니다.
이 피드백이 없으면 레이어는 실전에서 못 씁니다.

### 3.3 코드(chord)

동시에 눌린 키 조합을 별도 테이블(기본 8개)에서 조회합니다. 코드가 매칭되면 개별 키의
`TAP`은 취소됩니다. 슬롯 96개로 부족할 때를 위한 여유이자, "1+2 동시 = 패닉 정지" 같은
안전 동작을 넣을 자리입니다.

## 4. 액션 모델 — 무엇을 어디서 실행하는가

액션은 **장치에서 끝나는 것**과 **호스트가 받아 처리하는 것**으로 나뉩니다. 이 분리가
이 설계의 핵심입니다.

| ID | 액션 | 실행 위치 | 페이로드(3바이트) |
| --- | --- | --- | --- |
| 0 | `NONE` | — | — |
| 1 | `KEY` | 장치 | modifiers, keycode, 반복 플래그 |
| 2 | `CONSUMER` | 장치 | usage 하위/상위 바이트 (볼륨·재생·밝기) |
| 3 | `MOUSE_BUTTON` | 장치 | 버튼 마스크, 클릭 수 |
| 4 | `MOUSE_MOVE` | 장치 | dx, dy (int8), 스텝 |
| 5 | `MOUSE_WHEEL` | 장치 | delta (int8) |
| 6 | `LAYER_MOMENTARY` | 장치 | 레이어 번호 |
| 7 | `LAYER_TOGGLE` | 장치 | 레이어 번호 |
| 8 | `SEQUENCE` | 장치 | 장치 매크로 테이블 인덱스 |
| 9 | `HOST` | **호스트** | 토큰 ID |
| 10 | `LED_SCENE` | 장치 | 팔레트 인덱스 |
| 11 | `DELAY` | 장치 | 10 ms 단위 지연 (장치 매크로 스텝 전용) |

**장치 액션**은 EEPROM만 있으면 되므로 앱 없이 동작하고 지연이 1 ms 미만입니다.
단축키, 볼륨, 마우스 클릭처럼 자주 쓰는 것들이 여기 들어갑니다.

**호스트 액션**(`HOST`)은 장치가 `EV t=host tok=N` 한 줄만 흘려보내고 실제 일은 PC가 합니다.
긴 텍스트, 이미지 클립보드 붙여넣기(현재 앱의 기존 기능), 셸 명령, 조건 분기, 앱별 동작처럼
2 KB SRAM과 1 KB EEPROM에 넣을 수 없는 것 전부가 여기 들어갑니다.

> 왜 나누는가: 전부 장치에 넣으면 메모리에 막히고, 전부 호스트에 넣으면 앱이 죽었을 때
> 키패드가 벽돌이 됩니다. 자주 쓰고 짧은 것은 장치, 길고 똑똑한 것은 호스트가 맡습니다.

### 4.1 호스트 액션 registry

호스트 액션은 타입 문자열로 registry에서 찾습니다. 새 종류를 추가할 때 기존 코드를 고치지
않습니다.

```python
@register("clipboard_image")
class ClipboardImageAction(Action):
    def run(self, ctx: ActionContext) -> None: ...
```

기본 제공: `hotkey`, `text`, `clipboard_image`, `shell`, `sequence`, `delay`, `layer`, `noop`.

## 5. 메모리 예산 — 왜 이 형태인가

ATmega32u4는 SRAM 2.5 KB, EEPROM 1 KB입니다. 이 두 숫자가 자료구조를 결정했습니다.

**액션은 4바이트 고정**(`type`, `a`, `b`, `c`)입니다. 가변 길이로 만들면 파서가 커지고
EEPROM 조각화가 생깁니다.

EEPROM 배치:

| 영역 | 크기 | 내용 |
| --- | --- | --- |
| 헤더 | 16 B | magic, schema version, 밝기, 플래그, CRC16 |
| 키맵 | 384 B | 4 레이어 × 8 키 × 3 제스처 × 4 B |
| 코드 | 40 B | 8개 × (키 마스크 1 B + 액션 4 B) |
| LED 팔레트 | 12 B | 4 레이어 × 1 픽셀 × RGB |
| 장치 매크로 | 480 B | 인덱스 테이블 + 3 B 스텝 |
| 합계 | 932 B | (1024 B 안) |

팔레트는 `MK_LED_COUNT`에서 자동으로 따라옵니다. 픽셀을 8개로 늘리면 96 B가 되어 합계가
1016 B로 올라가는데, 그래도 1 KB 안입니다.

**키맵을 RAM에 미러링하지 않습니다.** 키를 누른 순간 EEPROM에서 4바이트를 직접 읽습니다.
AVR EEPROM 읽기는 바이트당 약 4 사이클이라 사람 손가락 기준으로는 즉시나 마찬가지고,
이 결정 하나로 SRAM 384바이트를 아낍니다. 절약한 SRAM은 LED 프레임 버퍼(1 × 3 B)와
시리얼 라인 버퍼(96 B)가 씁니다.

## 6. LED 파이프라인

WS2812B 1개는 단순한 장식이 아니라 **상태 표시 장치**입니다. 4개 소스를 우선순위로 합성해
하나의 픽셀에 얹습니다.

```
우선순위 높음
  1. 키 누름 피드백      흰색 플래시, 120 ms 감쇠
  2. 레이어 표시         활성 레이어 ≠ 0 이면 레이어 색조
  3. 호스트 앰비언트     AgentPet 상태 → 색 + 효과
  4. 기본 씬            프로필 팔레트
우선순위 낮음
```

**픽셀이 하나라는 제약이 여기서 드러납니다.** 8픽셀이면 "3번 키를 눌렀다"를 세 번째 픽셀로
보여줄 수 있었지만, 1픽셀에서는 어느 키든 같은 반짝임입니다. `LedController::notePress`는
모든 키의 누름을 마지막 픽셀 하나로 모읍니다. 픽셀 수보다 키가 많을 때 누름을 그냥 버리면
대부분의 키가 아무 피드백도 못 주기 때문입니다. 실질적으로 이 픽셀의 주된 역할은
**레이어 표시**입니다.

### 6.1 소유권과 워치독

호스트가 `LED mode=host`로 앰비언트 계층을 잡습니다. 호스트에서 3초 넘게 아무 프레임도
오지 않으면 장치가 자동으로 로컬 씬으로 되돌립니다. **앱을 껐을 때 LED가 마지막 색으로
얼어붙는 사고**를 막는 장치입니다.

### 6.2 AgentPet 상태 매핑

AgentPet `State`를 색과 효과로 옮깁니다. `severity`가 `warning`/`critical`이면 상태보다
우선합니다.

| State | 색 | 효과 |
| --- | --- | --- |
| `idle` | 어두운 청록 | solid |
| `sleeping` | 아주 어두운 파랑 | breathe (느림) |
| `thinking` / `planning` | 보라 | breathe |
| `reading` | 파랑 | solid |
| `editing` | 초록 | solid |
| `running` | 청록 | pulse |
| `researching` | 남색 | breathe |
| `waiting` | 노랑 | blink (느림) |
| `approval` | 주황 | blink (빠름) |
| `success` | 초록 | flash 후 감쇠 |
| `warning` | 주황 | solid |
| `error` | 빨강 | blink |
| `offline` | 소등 | — |
| `overloaded` | 자홍 | pulse (빠름) |

`progress` 필드가 있으면 픽셀을 순서대로 채우고 마지막 하나를 나머지만큼 어둡게 합니다.
픽셀이 하나뿐인 지금은 이게 **밝기 변조**로 떨어집니다 — `progress=0.3`이면 상태 색이 30%
밝기입니다. 거칠지만 작업이 돌아가고 있다는 것은 읽힙니다. 스트립으로 늘리면 같은 코드가
그대로 진행률 바가 됩니다.

### 6.3 전력 상한

픽셀 1개는 흰색 최대에서 약 60 mA입니다. MCU 30 mA를 더해도 90 mA로 USB 500 mA 예산의
18%밖에 안 쓰므로 전력은 실질적인 제약이 아닙니다. 그래도 `LedController`는 프레임을
내보내기 전에 채널 합을 계산해 `MK_LED_MAX_MILLIAMPS`(현재 100 mA)로 스케일 다운합니다.
이 코드를 남겨 둔 이유는 `MK_LED_COUNT`를 올리는 순간 다른 곳을 고치지 않아도 바로
안전장치가 되기 때문입니다. 자세한 계산은 [`HARDWARE.md`](HARDWARE.md#전력-예산)에 있습니다.

## 7. 매크로 레코딩 — "동작을 로깅해서 키에 할당"

호스트 앱의 recorder가 실제 입력을 잡아 액션 시퀀스로 바꿉니다.

```
캡처            정규화              편집             할당
pynput 리스너 → RawEvent 스트림 → ActionSequence → 슬롯(키·제스처·레이어)
```

정규화 단계에서 하는 일:

- **modifier 접기** — `ctrl↓ c↓ c↑ ctrl↑` 를 `hotkey ctrl+c` 하나로 합칩니다.
- **지연 양자화** — 사람의 들쭉날쭉한 간격을 10 ms 단위로 반올림하고, 임계값(기본 40 ms)
  미만은 버립니다. 그대로 재생하면 사람 타이핑 속도로 느리게 재생됩니다.
- **연속 문자 병합** — 낱개 키 입력을 `text` 액션 한 개로 묶습니다. 15자 문자열이
  30개 이벤트가 되는 것을 막습니다.
- **자기 입력 제외** — 레코딩 중 매크로 키패드 자신이 보낸 입력은 무시합니다. 아니면
  녹화가 자기 자신을 잡아먹습니다.

정규화된 결과가 장치 액션 하나로 표현 가능하면(단일 단축키 등) EEPROM에 굽고, 아니면
호스트 액션으로 저장한 뒤 그 슬롯에는 `HOST` 토큰만 넣습니다. **사용자는 이 구분을
신경 쓸 필요가 없고, 앱이 알아서 배치합니다.**

## 8. 호스트 앱 구조

```
macrokey/
├── config/      profile 모델 · 저장소 · 스키마 마이그레이션
├── device/      시리얼 transport · 프로토콜 코덱 · 포트 탐색 · 자동 재연결
├── actions/     registry + 실행기 (hotkey, text, clipboard_image, shell, …)
├── recorder/    입력 캡처 · 정규화
├── led/         팔레트 · 합성 · AgentPet 클라이언트 · 송출 서비스
├── backends/    OS별 백엔드 (키보드, 클립보드, 활성 창)
└── ui/          PySide6 화면 (얇게 유지, 교체 가능)
```

핵심 규칙은 **`ui/`를 제외한 어느 모듈도 UI 툴킷을 import 하지 않는다**입니다. 헤드리스 실행
(`macrokey daemon`)은 이 규칙 덕분에 그냥 됩니다. 이 규칙의 값어치는 실제로 확인됐습니다 —
0.4.0에서 Tkinter를 PySide6로 갈아끼울 때 `ui/app.py` 한 파일만 다시 썼고 나머지 계층은
한 줄도 건드리지 않았습니다.

PySide6는 **선택 의존성**입니다. Qt는 설치 용량이 크고, 데스크톱이 없는 장비에서 `push`나
`daemon`만 돌리는 경우가 이 프로젝트의 실제 사용 경로에 있기 때문입니다. 없으면 `macrokey gui`
하나만 설치 안내와 함께 실패하고 나머지 명령은 그대로 동작합니다.

장치와 통신하는 작업(connect · push · pull)은 이전과 같이 평범한 스레드에서 돌고, 결과는 Qt
시그널로 GUI 스레드에 전달됩니다. 워커 스레드에서 emit한 시그널을 이벤트 루프가 GUI 스레드에서
받아 주기 때문에 슬롯 안에서는 위젯을 직접 만져도 안전합니다. Tkinter의 `root.after(0, ...)`가
하던 일을 시그널이 대신합니다.

플랫폼 백엔드도 같은 이유로 분리했습니다. 기존 앱은 `win32clipboard`를 최상위에서 import 해서
Windows 밖에서는 이미지 기능이 통째로 죽었습니다. 이제 Windows/Linux/macOS 백엔드를
런타임에 고르고, 없는 기능은 그 액션 하나만 실패합니다.

## 9. 확장 지점 정리

| 늘리고 싶은 것 | 건드릴 곳 |
| --- | --- |
| 새 호스트 액션 | `actions/`에 클래스 하나 + `@register` |
| 새 장치 액션 | `ActionTypes.h`에 ID 추가 + `KeyEngine`의 dispatch 테이블 한 줄 |
| 새 LED 효과 | `LedEffects.h`에 함수 하나 + 효과 테이블 한 줄 |
| 새 이벤트 소스 | AgentPet 이벤트 프로토콜로 소켓에 JSON 한 줄 쓰기 |
| 키 개수 변경 | `Config.h`의 `MK_KEY_COUNT`와 핀 배열 |
| 레이어 수 변경 | `Config.h`의 `MK_LAYER_COUNT` (EEPROM 예산 확인) |
| LED 개수 변경 | `Config.h`의 `MK_LED_COUNT` + 호스트 `config/model.py`의 `LED_COUNT` (같은 값) |
| 다른 보드 | `Config.h` 핀 맵 (Leonardo/Micro는 그대로, RP2040은 HAL 교체) |

## 10. 의도적으로 하지 않은 것

- **키 매트릭스 스캔** — 8개는 GPIO 직결로 충분하고, 매트릭스는 고스팅 처리 비용만 늘립니다.
  16키 이상으로 갈 때 `ButtonInput`만 교체하면 되도록 인터페이스는 분리해 두었습니다.
- **장치 내 긴 텍스트 저장** — EEPROM 1 KB에 문자열을 넣기 시작하면 금방 무너집니다.
  호스트 액션이 이 역할을 합니다.
- **양방향 프로필 자동 동기화** — 충돌 해소 규칙이 복잡해집니다. 호스트가 단일 진실
  공급원이고, 장치로는 명시적으로 write/commit 합니다.
- **암호화된 시리얼 링크** — 로컬 USB 링크이고 상대는 이미 HID 키보드입니다. 자물쇠를
  달 곳이 아닙니다.

## 11. 안전 고려

HID 장치는 정의상 사용자가 알아채기 어려운 입력을 보낼 수 있습니다. 이 프로젝트는
개인 도구이므로 다음을 지킵니다.

- 부팅 직후 2초 동안 입력을 보내지 않습니다(펌웨어 사고 시 재업로드 시간 확보).
- `SEQUENCE`/`HOST` 액션에는 최대 스텝 수와 총 실행 시간 상한이 있습니다.
- 레코더는 녹화 중임을 LED 전체 빨강 pulse로 표시합니다. 조용히 키 입력을 기록하지 않습니다.
- 레코딩 결과는 저장 전 사람이 읽을 수 있는 형태로 화면에 보여주고 확인을 받습니다.
