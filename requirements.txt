# Runtime — config app (GUI + recording)
PySide6>=6.6
pyserial>=3.5
pynput>=1.7
evdev>=1.6; platform_system=="Linux"

# Release packaging (./build_release.sh)
pyinstaller>=6

# Dev / tests (optional)
pytest>=7
