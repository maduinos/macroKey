> 만든 사람: maduinos<br>
> 문서 만든 날짜: 2026-05-30<br>
> https://maduinos.blogspot.com/

# Changelog

## 0.15.0 — 2026-09-04

키 편집 창이 **단축키 하나만** 묻습니다. "키 입력받기"는 이제 우분투 설정처럼
**실제 키보드를 직접 잡아서** 읽습니다. **펌웨어는 그대로입니다(`0.9.4`)** — 다시
flash할 필요 없습니다.

- **"키 입력받기"가 커널에서 키를 직접 읽습니다.** 예전에는 Qt가 이 창에 넣어주는
  키만 받았습니다. 데스크톱이 먼저 가져가는 키 — `super`, `alt+tab`, `print`,
  미디어키 — 는 창까지 오지 않으니 누를 수가 없었습니다. 이제 녹음기가 쓰던 입력
  노드(`/dev/input/event*`)를 같은 방식으로 읽습니다.

  받는 동안에는 키보드를 **독점**합니다(`EVIOCGRAB`). 그래서 (1) 데스크톱이 그 조합을
  가져가지 않고, (2) 지금 그 조합에 걸려 있는 동작이 같이 실행되지도 않습니다.
  커널이 주는 것은 레이아웃을 적용하기 전의 물리 키라 `shift+1`이 `!`가 아니라
  `shift+1`로 저장됩니다. F13~F24, PrtSc, 메뉴키까지 전부 같은 방식으로 들어옵니다.

  키보드를 뺏는 기능이라 빠져나갈 길을 여러 개 뒀습니다: 수식키가 아닌 키를 처음
  누르면 종료, 창을 닫으면 종료, 30초 동안 아무 키도 없으면 종료. 이미 키가 눌려
  있으면 아예 잡지 않고(데스크톱 쪽에 ctrl이 눌린 채 남는 것을 피합니다), 패드로
  녹음이 진행 중이면 잡지 않습니다 — 잡으면 그 녹음이 조용히 키를 잃습니다.

  권한이 없거나 Linux가 아니면 예전처럼 이 창 안에서만 받고, 칸 아래 한 줄이 **왜
  그런지**를 말합니다.

- **패드가 보낼 수 없는 키는 삼키지 않고 이유를 말합니다.** 노트북 Fn, 한/영·한자,
  미디어키처럼 Keyboard.h에 코드가 없는 키는 칸에 그대로 채운 뒤 "이 키패드는 보낼 수
  없습니다"라고 알려줍니다. 아무 일도 일어나지 않아 키 입력을 놓친 것처럼 보이던
  것보다 낫습니다. (미디어키는 펌웨어가 `MK_USE_HID_PROJECT=0`으로 빌드되어 consumer
  page 자체가 없습니다.)

- **PrtSc·메뉴키가 녹음에서도 살아납니다.** 커널은 Print Screen을 `KEY_SYSRQ`로,
  메뉴키를 `KEY_COMPOSE`로 보고합니다. 토큰표에 없어서 `sysrq`라는, 아무 데서도
  키코드로 바꿀 수 없는 이름으로 나오던 것을 `printscreen`·`menu`로 매핑했습니다.

- **키 편집 창에서 "현재 위치에서 마우스 사용"과 "직접 한 동작을 재생"을 뺐습니다.**
  마우스 클릭·휠 버튼과 녹음 체크박스 두 개가 단축키 아래에 있었는데, 녹음 체크박스는
  메인 창 Recording 줄에 있는 것과 같은 설정의 복사본이었습니다. 녹음 설정은 메인
  창에 그대로 있습니다. 창이 닫힐 때 그 복사본을 다시 읽어오던 코드도 함께 지웠습니다.

  단, `double` 슬롯 녹음 방법("탭한 뒤 250 ms 안에 다시 눌러 3초 홀드") 안내가 이
  창에서는 사라졌습니다.

- **수식키 표기 순서를 저장 형식과 통일했습니다.** 직접 입력하든, 키보드에서 읽든,
  패드에서 되읽든 `ctrl+alt+shift+1` 하나로 읽힙니다.

## 0.14.1 — 2026-09-04

0.14.0이 지목한 원인이 틀렸습니다. 이 릴리스는 기록을 바로잡고, 진짜 원인을 도움말에
적습니다. **펌웨어는 그대로입니다(`0.9.4`)** — 다시 flash할 필요 없습니다.

- **"앱을 켜면 마우스 매크로가 조금씩 어긋난다"의 원인은 Windows 작업 표시줄이었습니다.**
  매크로가 작업 표시줄 아이콘을 클릭하고 있었고, Windows 11은 아이콘을 기본으로
  **가운데 정렬**합니다. 열려 있는 앱이 하나 늘면 아이콘이 전부 옆으로 밀립니다 —
  macroKey를 켜는 것 자체가 그 하나입니다. 겨냥한 자리에 다른 아이콘이 와 있으니
  매크로는 이웃을 누릅니다.

  재생 거리가 달라진 게 아니라 **목표 좌표가 움직인 것**이었습니다. 앱을 켜고 끄는 것과
  정확히 연동되고, 어긋나는 양이 작고, 빠른 이동에서 눈에 띈다는 점이 0.14.0에서 세운
  가속 가설과 표면적으로 같아 보였습니다. 증상이 가설에 들어맞는다고 원인이 확인된 것은
  아니라는 쪽이 이번 교훈입니다.

  고치는 법은 설정입니다: **설정 > 개인 설정 > 작업 표시줄 > 작업 표시줄 동작 >
  작업 표시줄 맞춤을 왼쪽으로** 바꾸고 겨냥할 앱을 고정하세요. 왼쪽 정렬에서는 고정된
  아이콘이 자리를 지키고 새 창은 그 오른쪽에 붙으므로, 앱을 켜든 끄든 좌표가 변하지
  않습니다. **도움말 > Mouse macro accuracy**에 이 내용을 적었습니다.

- **가속 관련 문구가 단정하던 것을 되돌렸습니다.** 0.14.0은 "빠른 이동은 매크로가 짧게
  떨어집니다"라고 썼는데, 측정한 것은 리포트 예산에 여유가 없다는 사실이지 실제로 짧게
  떨어진 사례가 아니었습니다. 이제 "짧게 떨어질 수 있습니다"로 말합니다.

- **0.14.0의 페이싱 변경은 그대로 둡니다.** USB 프레임을 여유 0.05 ms로 정조준하던 것을
  0.61 ms로 넓힌 것은 측정된 개선이고, 원인 규명이 아니라 견고화로 성격만 바뀝니다.
  펌웨어 `0.9.4`는 그대로입니다.

## 0.14.0 — 2026-09-04

빠른 마우스 매크로가 그린 곳보다 짧게 떨어지던 문제를 고치고, 스스로 설치되는
업데이트가 무엇을 했는지 말하게 합니다. **펌웨어가 `0.9.4`로 올라갑니다** — 패드를
꽂으면 앱이 알아서 굽습니다. 프로필 스키마는 그대로라 매크로는 보존됩니다.

- **앱이 켜져 있을 때와 꺼져 있을 때 마우스 매크로가 다른 곳에 찍혔습니다.** 증상은
  "앱을 켜두면 매크로가 조금 덜 간다"였고, 빠르게 그린 대각선 이동에서 났습니다.

  > **정정 (0.14.1):** 이 진단은 틀렸습니다. 아래 페이싱 문제는 실재하지만 저 증상의
  > 원인이 아니었습니다. 진짜 원인은 Windows 11의 가운데 정렬 작업 표시줄이고,
  > 0.14.1 항목에 있습니다. 아래 변경은 원인 수정이 아니라 재생 여유 확보로 읽으세요.

  재생 자체는 앱과 무관합니다 — 패드가 혼자 합니다. 하네스로 재생 경로를 돌려보면
  MCU가 아무리 바빠도 패드가 내보내는 델타는 한 톨도 바뀌지 않습니다. 리포트 250개,
  각 2 counts, 합계 500. 바뀌는 건 **속도**뿐입니다. 그런데 가속이 켜진 데스크톱은
  느린 이동에 더 작은 배수를 곱하므로, 늘어진 제스처는 짧게 착지합니다.

  왜 늘어지느냐가 진짜 원인이었습니다. `emitMove`가 리포트를 뿌릴 때 예산을
  `overMs / MK_HID_POLL_INTERVAL_MS`로 잡았는데, 이건 **USB 프레임 그 자체**입니다.
  50 ms 슬라이스를 50개 프레임에 리포트 50개 — 여유 0.05 ms. 프레임은 바닥이지
  예산이 아닙니다. 픽셀 비트뱅이 인터럽트를 잠깐 막거나, 에디터가 시리얼 포트를 열어
  USB 장치가 바빠지면 그 리포트는 이미 늦은 것이고, 데드라인이 절대 시각이라 따라잡을
  수도 없습니다.

  이제 프레임의 3/2로 뿌립니다. 같은 50 ms에 리포트 33개, 간격 1.5 ms, **여유
  0.61 ms — 12배**. 이동 거리는 그대로고, 리포트당 델타만 2에서 3~4로 굵어집니다.
  느린 이동은 애초에 상한에 닿지 않아 아무것도 달라지지 않습니다.

  같은 종류의 버그를 RP2040에서 한 번 겪었는데(10 ms 폴링에서 제스처가 열 배로
  늘어짐), 그때는 "제자리에 느리게 찍힌다"로 판단했습니다. 그건 가속이 꺼져 있을
  때만 참이었습니다.

- **가속을 끄라고 권하는 창이 틀린 말을 하고 있었습니다.** "녹화 속도로 재생하므로
  대체로 상쇄된다"고 안심시켰는데, 정확히 상쇄되지 않는 경우가 위의 그 경우입니다.
  이제 빠른 이동에서는 상쇄되지 않고 매크로가 짧게 떨어진다고 말합니다.

- **한 번 거절하면 다시 묻지 않던 것을, 근거가 생겼을 때 한 번만 다시 묻습니다.**
  시작할 때 뜨는 질문은 가리킬 대상이 없는 상태에서 던지는 것이고, 거기서 누른
  "아니오"는 영구히 기억됩니다. 이제 실제로 영향받는 매크로를 녹화한 직후, 무엇이
  달라졌는지와 함께 딱 한 번 더 묻습니다. 두 번은 없습니다 — 계속 되묻는 건 거절을
  존중하는 게 아닙니다.

- **업데이트가 무엇을 했는지 볼 수 있습니다.** 앱과 펌웨어 둘 다 아무도 안 볼 때
  스스로 설치되는데, 지금까지 남는 건 버전 번호뿐이었습니다. 자기가 바꾸기로 하지
  않은 프로그램이 갑자기 다르게 동작하면, 그건 새 버그처럼 보입니다.

  릴리스 본문이 업로드한 파일 목록이던 것을 CHANGELOG 항목으로 바꿨고(`---` 아래가
  파일 안내), 앱이 그 위쪽을 설치 알림에 그대로 보여줍니다. `Release.notes`는 이미
  받아서 담아두고 아무도 안 쓰던 값이었습니다.

  펌웨어 쪽은 상태바 한 줄이라 지나가면 끝이었습니다. 케이블을 꽂았다는 이유로
  키패드를 몇 초 가져가는 일이니, 이제 전/후 버전과 함께 알림으로 말합니다.

- **도움말 > macroKey 정보**가 생겼습니다. 버전과 만든 곳(maduinos), 그리고
  <https://maduinos.blogspot.com/> 링크. 앱이 바깥을 가리키는 유일한 자리입니다.

## 0.13.0 — 2026-09-04

앱에 아이콘이 생겼고, Windows에서 소스로 빌드하는 길이 막혀 있던 것을 뚫습니다.
펌웨어는 그대로입니다(`0.9.3`) — 다시 flash할 필요 없습니다.

- **앱 아이콘을 추가합니다.** 지금까지 `assets/`는 비어 있었고, 빌드 스크립트는 거기 있을지도
  모르는 파일을 조용히 건너뛰고 있었습니다. 그래서 Windows 작업표시줄에도 Linux 창에도
  툴킷 기본 아이콘만 떴습니다.

  그림은 제품을 위에서 본 모습입니다 — 8키를 4×2로, 그 아래 WS2812B 픽셀 하나.
  키 하나와 픽셀만 빨간 건 이 패드의 유일하게 비직관적인 동작을 그림에 담은 것입니다:
  키를 3초 누르면 그 키에 녹음이 시작되고 픽셀이 빨강으로 바뀝니다.

  32px 미만은 4열이 뭉개져 2×2 단순형을 따로 그립니다. 그리는 사람이 따로 손볼 일은
  없습니다 — 그림은 `tools/make_icon.py`가 생성하고, 각 크기를 4배 캔버스에서 그려
  줄입니다. 256px를 16px로 한번에 줄이면 키가 서로 번져 무엇인지 알 수 없게 됩니다.

  Linux에서는 PyInstaller가 `--icon`을 무시하므로(실행 파일에 아이콘이란 개념이 없습니다)
  앱이 실행 중에 직접 창 아이콘을 지정합니다. 대화상자와 메시지 박스도 같이 따라갑니다.

- **Windows Git Bash에서 `./build_release.sh`가 방금 설치한 의존성을 없다고 말하며 죽었습니다.**
  스크립트는 PyInstaller에 넘기는 경로를 전부 `cygpath -w`로 변환하면서 `PYTHONPATH`만
  POSIX 경로(`/c/Users/...`)로 내보내고 있었습니다. MSYS는 네이티브 바이너리로 가는
  **인자**를 경로 모양이면 고쳐주지만 **환경 변수**는 건드리지 않습니다. 그래서 pip는
  올바른 자리에 설치하고, 바로 다음 줄의 import 검사는 그 자리를 보지 못해 실패합니다 —
  "설치는 됐는데 없다고 한다"는 이상한 모양입니다.

  Linux에는 `cygpath`가 없으므로 동작이 그대로입니다. 회귀 테스트는 `cygpath` 스텁을
  PATH에 놓아 그 상황을 Linux에서 재현합니다.

## 0.12.1 — 2026-09-04

녹음 중에 클릭이 사라지던 문제를 끝까지 고칩니다. 펌웨어는 그대로입니다(`0.9.3`) —
다시 flash할 필요 없습니다.

- **에디터 창의 사각형이 앞 창으로 간 클릭을 계속 버리고 있었습니다.** 증상은 "엑셀에서
  칸을 클릭해 이동하는 매크로가 한 칸 건너 한 번씩만 먹는다"였습니다.

  0.12.0 다음에 "이 창이 활성일 때만 사각형을 적용한다"로 좁혔지만, 그것으로 부족했습니다.
  기기에서 읽어낸 Windows 녹음은 포인터가 일곱 군데에 멈췄는데 클릭은 네 개뿐이고,
  **사라진 세 개는 전부 에디터 창 좌표 안, 살아남은 네 개는 전부 밖**이었습니다. 그
  녹음은 에디터가 엑셀 **뒤에** 있는 상태에서 만들어졌습니다 — 활성 조건대로면 사각형이
  꺼져 있었어야 하는 바로 그 상황입니다. 마지막 스텝이 결정적입니다: 매크로는 칸을
  클릭하지도 않고 그 자리에서 `=sum`을 칩니다.

  그래서 사각형을 없앴습니다. 좌표로는 그 클릭이 누구에게 갔는지 알 수 없고, 틀렸을 때
  사람이 실제로 한 입력이 조용히 지워집니다. 대신 **Qt가 이 프로세스의 위젯에 press를
  실제로 전달했는지**를 봅니다. 그건 추측이 아니라 증거입니다 — 앞 창으로 간 클릭은
  여기 도착하지 않습니다. 창·대화상자·메뉴 어디를 눌렀든 잡히고, 좌표도 플랫폼별 코드도
  필요 없습니다.

  판정은 녹음이 끝날 때 합니다. 전역 후크가 Qt보다 **먼저** 클릭을 보기 때문에, 그
  순간에는 아직 맞춰볼 증거가 없습니다. 시간 비교는 한 방향입니다: 이 창의 이벤트보다
  **앞선** 클릭만 같은 클릭일 수 있습니다. 양쪽으로 열어두면 "창을 눌러 앞으로 꺼낸 뒤
  곧바로 대상 앱을 클릭"하는 흔한 동작에서 뒤엣것까지 함께 지워집니다.

  press를 버릴 때는 짝인 release도 같이 버립니다. 남겨두면 `normalize`가 그것을 "캡처
  이전에 시작된 드래그의 꼬리"로 읽고 건너뛰어, 뒤에 무엇이 오느냐에 따라 결과가
  달라집니다.

  evdev에도 이제 같은 규칙이 적용됩니다. 좌표가 없어서 이 클릭들을 아예 걸러내지 못하던
  쪽이라, 두 백엔드가 처음으로 같은 동작을 합니다.

- **클릭이 버려지면 로그에 남습니다.** 사라진 클릭은 밖에서 보면 어느 이유든 똑같이
  보입니다 — 에디터가 받은 클릭이어서 버린 것인지, 키패드 자기 HID 에코를 막는 150 ms
  방어막에 걸린 것인지. 이제 둘 다 이유와 함께 기록되므로, 끝난 녹음만 가지고도 구분할
  수 있습니다.

## 0.12.0 — 2026-09-03

대각선 마우스 이동이 끝까지 가지 않던 문제를 고치고, 앱과 펌웨어가 스스로 최신을
따라가게 합니다. 펌웨어는 그대로입니다(`0.9.3`) — 다시 flash할 필요 없습니다.

- **에디터 창 위로 지나간 마우스 이동이 통째로 사라지고 있었습니다.** 녹음 중 이 창을
  클릭한 것이 매크로 스텝이 되지 않도록 창 사각형 안의 이벤트를 버리는데, 그 필터가
  클릭·휠뿐 아니라 **포인터 위치 표본**에도 걸려 있었습니다. 포인터가 창을 가로지르는
  동안의 이동량이 기록되지 않으니, 매크로는 딱 그만큼 못 미친 곳에서 멈춥니다.

  화면을 가로지르는 대각선은 가운데 놓인 창을 거의 항상 지나갑니다. 그래서 증상이
  "대각선만 제대로 안 간다"로 보였습니다. 화면 위쪽을 따라가는 수평 이동은 창을
  비껴가므로 멀쩡했고요. 재현 스크립트로 확인한 손실은 (900, 600) 이동에서
  (290, 193) — 3분의 2가 날아갑니다.

  **pynput 경로만의 문제입니다**: 좌표를 보고하는 캡처는 그것뿐입니다. Windows는 항상
  이 경로를 쓰고, Linux는 `input` 그룹이 없어 evdev로 잡지 못하고 X11로 내려갔을 때만
  해당됩니다. evdev는 포인터 좌표 자체가 없어서 이 필터가 적용된 적이 없습니다 —
  같은 대각선이 Linux에서는 멀쩡했던 이유입니다.

  클릭과 휠 필터는 그대로입니다. 창 위를 **지나간 것**은 제스처이고, 창을 **누른 것**은
  에디터 조작입니다.

- **패드 키 이벤트가 진행 중인 마우스 이동을 통째로 삼키고 있었습니다.** pynput 경로에만
  있는 self-echo 방어막(150 ms)이 *이동 슬라이스 전체*를 버렸습니다. 이동은 한 개의
  이벤트가 아니라 녹음기가 직접 더한 50 ms어치 이동량이라, 150 ms를 가리려다 그보다
  훨씬 많이 지웠습니다 — 드래그 (312, 312) 중간에 패드 키 하나가 끼면 200 카운트가
  사라집니다. 판정 기준이 슬라이스의 *시작* 시각이어서 패드 이벤트보다 **먼저 시작한**
  슬라이스도 경과가 음수로 나와 "150 ms 미만"에 걸려 같이 날아갔습니다.

  방어막은 이제 키·버튼·휠 같은 **개별 이벤트에만** 걸리고, 경과가 음수인 것은 통과시킵니다.
  에코는 그런 식으로 도착할 수 없습니다. evdev는 패드의 입력 노드를 아예 건너뛰므로 무관합니다.

- **pynput에서 키 입력이 이동을 flush하지 않아 녹음 순서가 뒤바뀌었습니다.** "저기로 옮기고
  타이핑"이 "타이핑하고 저기로 옮김"으로 돌아왔습니다 — 매크로는 포인터를 옮기기 *전에*
  포커스를 갖고 있던 창에 입력합니다. 두 스텝 사이 간격도 잘못된 쪽에서 재어 300 ms가
  600 ms가 됐습니다. evdev는 예전부터 모든 키 앞에서 flush했고, 이제 양쪽이 같습니다.

- **클릭 직전의 작은 이동이 노이즈로 버려지고 있었습니다.** `MOTION_DEAD_ZONE`(6카운트)은
  "마우스에 얹어둔 손"을 거르는 값인데, 그 필터가 클릭·휠이 유발한 flush에도 걸려 있었습니다.
  4카운트 밀고 클릭하면 밀기가 사라지고, 클릭은 실제로 누른 자리에서 4카운트 떨어진 곳에
  떨어집니다 — 이동이 끝까지 안 가는 것과 같은 불만입니다.

  버튼과 휠은 **포인터를 겨냥한** 이벤트라 그 앞의 이동은 아무리 작아도 조준입니다. 이제
  거기서는 필터를 걸지 않습니다. 키 입력은 포인터를 겨냥하지 않으므로 그대로 두었습니다 —
  풀었더니 마우스에 손을 얹고 타이핑할 때 글자마다 미세 이동이 스텝으로 쌓였습니다.
  데드존은 원래 자리인 rest 타임아웃(뒤에 아무것도 따라오지 않은 이동)과 녹음 종료 시점에
  그대로 남습니다.

- **앱이 새 릴리스를 스스로 받아 설치합니다.** 시작할 때 GitHub 릴리스를 한 번 보고,
  더 높은 버전이 있으면 내려받아 실행 파일을 교체합니다. 적용은 다음 실행이며 —
  녹음 중인 창을 마음대로 재시작하지 않습니다 — 다 되면 그렇게 안내합니다.

  실행 중인 바이너리 교체는 이름 바꾸기로 합니다. Linux는 열려 있는 inode가 그대로
  살아 있어 `os.replace` 한 번이면 되고, Windows는 실행 중인 `.exe`를 *덮어쓸* 수는
  없어도 *이름을 바꿀* 수는 있으므로 옛 것을 `.old`로 밀어내고 새 것이 그 이름을
  차지합니다. 남은 `.old`는 다음 시작 때 지웁니다.

- **패드 펌웨어가 앱보다 낮으면 연결할 때 올려 씁니다.** 프로필 레이아웃은 앱과 펌웨어가
  같은 스키마로 합의해야 하는 값이라, 한쪽만 뒤처지면 아무것도 실패하지 않으면서 조용히
  어긋납니다. 그래서 자동입니다.

  **보통은 인터넷이 필요 없습니다.** 보드별 이미지가 이미 실행 파일 안에 있습니다.
  네트워크는 새 *앱*을 가져올 때, 그리고 이 빌드에 이미지가 아예 없을 때만 씁니다 —
  펌웨어는 앱을 따라옵니다.

- **버전은 이미지 안에서 읽습니다.** 파일 이름도 릴리스 태그도 아니고, 펌웨어가 `HELLO`로
  출력하는 `fw=` 문자열을 `.hex`/`.uf2`에서 그대로 찾습니다. 펌웨어 버전과 앱 버전은
  서로 다른 시계라, 태그로 짐작하면 이미 굽혀 있는 것을 다시 굽게 됩니다. 버전을 읽을 수
  없는 이미지는 "모른다"이며 업데이트로 제안하지 않습니다.

- **받은 파일은 릴리스의 `SHA256SUMS.txt`와 대조한 뒤에만 씁니다.** 해시가 맞지 않으면
  파일을 지우고 실패로 끝냅니다. 해시가 아예 게시되지 않은 자산은 받지 않습니다 —
  검사하는 이유가, 이 파일이 곧 키패드나 실행 중인 프로그램 위에 쓰인다는 것이니까요.

- 녹음 중·동기화 중·초기화 중이거나 이 키패드에 대한 질문이 떠 있는 동안에는 굽지 않고,
  **2초 뒤에 다시 봅니다**. "다음 연결에 다시"는 계획이 아니었습니다 — 케이블이 가만히
  있으면 연결은 평생 한 번뿐이고, 이 확인은 바로 그 연결 *도중에* 끝나므로(연결 워커에서
  돌아 `_connecting`이 아직 참) 거기서 버리면 멀쩡한 링크에서 업데이트가 아예 일어나지
  않습니다. 패드가 빠지면 재시도를 그만두고 잊습니다 — 굽을 보드가 없는 채로 재시도하면
  그냥 뽑았을 뿐인데 실패 대화상자가 뜹니다.

- 같은 보드의 같은 펌웨어 버전은 세션당 한 번만 확인합니다 — 케이블이 흔들려 재연결이
  반복돼도 같은 질문을 몇 초마다 네트워크에 던지지 않습니다.

- `macrokey update`(`--check`, `--firmware`, `--app`, `--offline`, `--yes`)와
  **도움말 > 업데이트 확인**. 자동 업데이트는 같은 메뉴에서 끌 수 있고,
  `MACROKEY_NO_UPDATE=1`은 네트워크 확인을 통째로 막습니다.

## 0.11.1 — 2026-09-02

RP2040에서 마우스 매크로가 녹화한 것보다 열 배 느리게 재생되던 문제를 고칩니다.
펌웨어를 다시 flash해야 합니다(`0.9.3`).

- **RP2040의 HID 엔드포인트가 10 ms마다 폴링되고 있었습니다.** arduino-pico가
  그렇게 기본값을 잡습니다:

      int usb_hid_poll_interval __attribute__((weak)) = 10;   // USB.cpp

  AVR 코어의 HID 엔드포인트는 1 ms입니다. 녹화된 50 ms짜리 움직임 한 조각은
  1 ms에 하나씩 리포트를 보내 재생되는데, 엔드포인트가 10 ms마다 열리면 리포트
  하나하나가 그만큼 기다리고 제스처는 그린 시간의 열 배로 늘어납니다. 포인터는
  결국 제자리에 도착하므로 **아무것도 실패하지 않습니다** — 증상은 "이 보드에서만
  마우스 매크로가 느리다"뿐이고, 그래서 32u4에서는 멀쩡했습니다.

  심볼이 weak인 이유가 스케치에서 덮으라는 것이라, 덮습니다. 실물에서 확인:
  `lsusb`의 HID 인터페이스 `bInterval`이 10에서 1로 바뀝니다.

- **재생이 엔드포인트가 감당할 수 있는 만큼만 보냅니다.** 폴링 간격이 무엇이든
  주어진 시간에 나갈 수 있는 리포트 수는 정해져 있고, 더 요청한다고 빨리 나가지
  않습니다 — 전부 엔드포인트를 기다리며 제스처만 늘어납니다. 이제 리포트 수를
  간격으로 나눠 상한을 두므로, 느린 보드에서는 리포트가 굵어질 뿐 재생 시간은
  맞습니다. 폴링 간격은 보드가 선언하고, 선언하지 않으면 컴파일 에러입니다.

- **테스트가 사용자의 실제 설정 파일을 고치고 있었습니다.** UI 테스트는 진짜
  `MainWindow`를 만들고, 진짜 창은 진짜 `Settings`를 읽고 씁니다. 포인터 가속
  테스트가 "괜찮습니다" 경로를 지나가면서 `pointer_accel_declined`를 저장했고,
  그 설정은 영구적입니다 — 스위트를 한 번 돌리면 개발자가 본 적도 없는 제안이
  조용히 꺼졌습니다. 실제로 이 저장소에서 그 일이 일어나 있었습니다. 이제
  `MACROKEY_CONFIG_DIR`로 임시 디렉터리를 가리키고, 우회되면 실패합니다.

- 창이 닫히는 중에 질문이 열려 있으면 "다시 묻지 않기" 가드가 풀리지 않던 문제.

## 0.11.0 — 2026-09-02

보드를 늘릴 수 있게 만드는 릴리스입니다. 펌웨어를 다시 flash해야 합니다(`0.9.2`) —
다만 이번 펌웨어는 동작이 같고, 재배치된 소스에서 다시 빌드됐다는 표시입니다.

- **키패드를 꽂으면 앱이 펌웨어를 설치합니다.** 어떤 보드인지 알아보고, macroKey 펌웨어가
  없으면 확인을 한 번 받은 뒤 직접 굽습니다. arduino-cli도 코어도 라이브러리도 필요
  없습니다 — 완성된 이미지가 실행 파일 안에 들어 있습니다(`macrokey flash`).

  RP2040은 부트로더가 USB 드라이브라 플래싱이 파일 복사입니다. Pro Micro는 Caterina의
  AVR109를 pyserial 위에 직접 구현했습니다 — avrdude를 번들하면 플랫폼마다 네이티브
  바이너리와 설정 파일을 들고 다녀야 하는데, 프로토콜이 그만큼 크지 않습니다.

  **한 번은 손이 필요합니다.** 공장 초기 상태의 보드에는 "부트로더로 가라"는 말을 들어줄
  코드가 없어서, 첫 플래싱만은 BOOTSEL이나 RST–GND 더블탭이 필요합니다. 대신 앱이 그
  문장을 띄우고 부트로더가 나타나는 것을 지켜보다 즉시 굽습니다 — 더블탭 직후에 명령을
  손으로 실행하던 타이밍 싸움이 사라집니다. 두 번째부터는 완전 자동입니다.

- **보드가 한 곳에 모였습니다.** 보드 분기가 `Config.h`·`Profile.h`·`Storage.cpp`·
  `SerialProtocol.cpp` 여섯 군데에 흩어져 있었습니다. 세 번째 보드를 넣으려면 여섯 곳을
  다 찾아야 했고, 놓친 한 곳은 빌드를 깨뜨리지 않았습니다 — 조용히 AVR 분기를 타서,
  없는 1 KB를 다 쓸 때까지 잘 도는 펌웨어가 됐을 겁니다.

  이제 펌웨어는 `firmware/src/boards/<board>.h` 하나, 앱은 `macrokey/boards.py`의 항목
  하나입니다. 프로필 레이아웃·USB 인식·펌웨어 이미지·플래싱 방법이 전부 거기서 파생되고,
  보드가 선언해야 할 심볼을 빠뜨리면 그 이름을 대는 컴파일 에러가 납니다. 새 보드 추가
  절차는 `docs/BOARDS.md`에 있습니다.

- **연결이 끊기면 가장 작은 보드로 되돌아가던 문제.** 프로필 파일은 자기가 어느 보드
  것인지 기록하지 않아서, 케이블을 뽑으면 앱이 1 KB AVR 레이아웃을 가정했습니다. RP2040
  패드의 매크로 32 레코드가 "21,801 중 0%"가 아니라 **"308 중 10%"** 로 표시됐고, 더
  나쁘게는 308을 넘는 녹음을 **거부**했습니다 — 패드에는 21,801개가 들어가는데.

  이제 마지막으로 연결한 보드를 기억합니다. 기억은 장치 클라이언트가 IDENT를 통과시킨
  자리에 걸려 있습니다. `MacroKeyApp.connect`에 걸었더니 CLI가 `app.device.connect`를
  직접 부르는 탓에 `macrokey info`로 RP2040에 붙고도 기억되지 않았습니다.

- **릴리스 바이너리에 CLI가 없던 문제.** PyInstaller가 묶는 것은 `main.py`인데 그 파일이
  `sys.argv`를 아예 읽지 않아서, `--version`도 `ports`도 `record`도 소스 체크아웃에서만
  닿았습니다. `--version`을 주면 값을 찍는 대신 창이 떴습니다. 이제 위임합니다.

  Windows 빌드는 `--windowed`라 콘솔이 없어 `sys.stdout`이 `None`입니다. 실행시킨
  콘솔에 붙어서 출력하고, 붙을 콘솔이 없으면(탐색기 더블클릭) 조용히 버립니다.

- **포인터 가속 질문이 자기 자신 안에서 무한히 열리던 문제.** 모달은 자기 이벤트 루프를
  돌리는데, 이 질문은 900 ms 타이머가 띄웁니다. 그 루프 안에서 같은 타이머가 또 돌면
  똑같은 창이 그 위에 열리고, 그 안에서 또 열립니다. 화면에는 구분할 수 없는 창만 쌓이고
  아무것도 이유를 말해주지 않습니다. 전역 재진입 가드를 두고, 자동으로 뜨는 질문은
  이벤트 루프를 세우지 않는 비모달로 바꿨습니다 — 자동 이벤트가 띄운 모달은 그동안
  재연결까지 멈춥니다.

## 0.10.0 — 2026-09-01

한국어 UI와, 매크로를 잃고도 되찾을 수 있게 만드는 릴리스입니다.
펌웨어를 다시 flash해야 합니다(`0.9.1`).

- **UI 언어를 고를 수 있습니다.** 도움말 > 언어에서 한국어와 영어 중 고르고, 기본값은
  시스템 로케일 자동 감지입니다. **바꾼 언어는 다음 실행부터** 적용되고 앱이 그렇게
  안내합니다. 창의 라벨과 툴팁, 그리고 버튼에 고정된 폭까지 전부 창을 만드는 시점에
  정해지기 때문입니다. 살아 있는 창을 그 자리에서 번역하면 절반만 바뀐 창에 잘린 버튼이
  남습니다.

  번역은 영어 원문을 키로 씁니다. 빠진 항목은 영어로 떨어지므로 번역이 코드를 조금
  뒤따라도 창이 깨지지 않습니다. 대신 그 관대함이 조용한 실패가 되지 않도록, 소스를
  파싱해서 모든 `tr()` 문자열이 표에 있는지, 표에 죽은 키가 없는지, 플레이스홀더가
  유실되지 않았는지를 테스트가 검사합니다.

- **번역된 라벨로 버튼 폭을 잡습니다.** Connect 버튼은 자기가 갖게 될 가장 긴 글자에
  맞춰 폭이 고정돼 있었는데, 그 계산이 영어 문자열을 직접 들고 있었습니다. 번역만 하고
  두었으면 "연결 해제"가 그대로 잘렸을 자리입니다 — 버튼은 줄어들기를 거부하지 않고
  글자를 생략하므로, 어디에도 오류가 나지 않고 그냥 말이 잘립니다. 슬롯 편집기의 녹음
  안내문도 영어 네 줄로 높이가 박혀 있어, 이제 실제 줄바꿈을 재서 아래 버튼이 문장을
  덮지 않게 합니다. 툴바와 슬롯 편집기를 언어 × 글꼴 크기로 교차 검증합니다.

- **Windows에서도 포인터 가속을 끌 수 있습니다.** 이전에는 Linux가 아니면 설정을 읽지
  못한다고 답해서, Windows에서는 이 기능이 아예 없었습니다. 이제 "포인터 정확도 향상"을
  직접 다루고, 되돌리는 방법도 플랫폼에 맞게 안내합니다. 속도 임계값은 건드리지 않고
  가속만 끕니다 — 그건 사용자가 따로 맞춘 다른 설정입니다.

- **가속을 끄겠냐는 물음의 기본 답이 예입니다.** 이 대화상자는 마우스를 녹음할 때만
  뜨고, 그 목적에는 flat이 더 나은 설정이며, 본문이 부작용과 되돌리는 명령을 이미 다
  알려줍니다. 권하는 답이 한 번 더 누르게 만들 이유가 없습니다.

- **이전 버전을 열 세대 보관합니다.** 한 세대는 정작 그게 필요한 상황에서 복구 수단이
  되지 못했습니다. 초기화는 직전 프로필을 백업 슬롯에 제대로 넣습니다 — 그리고 17초 뒤의
  평범한 저장 한 번이 그걸 도로 밀어냅니다. 아무도 모르는 사이에 일어난 일을 되돌리려면,
  다음 저장까지만 살아 있는 사본은 사본이 아닙니다. 초기화 대화상자가 하던
  "이전 버전 복원에서 꺼낼 수 있다"는 약속이 이제 실제로 지켜집니다.

  복원은 목록에서 고르는 방식으로 바뀌었습니다. 시각만으로는 고를 수 없습니다. 사고
  직전과 직후의 사본이 몇 초 차이라서, 둘을 가르는 건 한쪽에만 매크로가 남아 있다는
  사실뿐입니다. 그래서 각 사본이 "녹음된 매크로 2개"인지 "비어 있음"인지 함께 보여줍니다.

- **패드가 프로필을 잃은 것과 다른 프로필을 든 것을 구분합니다.** 바이트를 비교하면 둘은
  똑같이 "다름"이지만 정답은 정반대입니다. 패드가 비어 있고 이 컴퓨터에 내용이 있으면
  대화상자가 그 사실을 이름으로 말하고, 기본 버튼이 **보내기** 쪽으로 갑니다. 그전에는
  매크로를 지우는 "가져오기"가 첫 버튼이었습니다.

- **패드가 스스로 초기화한 사실이 호스트에 도달합니다.** 펌웨어는 저장된 프로필을 읽지
  못하면 공장 기본값을 덮어쓰고 그것을 `LOG`로 알렸습니다 — `setup()`에서 딱 한 번.
  그런데 호스트는 포트를 열고 보드가 안정되기를 기다린 뒤 그동안 도착한 것을 버리고
  `IDENT`를 보냅니다. 부팅에 한 번만 나가는 것은 그래서 아무도 보지 못했고, 패드가
  기억을 잃어도 남는 기록이 없었습니다. 이제 그 사실을 래치해 `HELLO reset=1`로 보냅니다.
  `HELLO`는 `IDENT`마다 다시 나가고, 키가 없으면 아니라는 뜻이라 구형 호스트는 그대로
  동작합니다.

- **버튼의 말줄임표를 정리했습니다.** `…`는 누르면 바로 실행되지 않고 **입력을 더 받는다**는
  뜻입니다. 가져오기·내보내기·이전 버전 복원(경로를 받음)과 동기화(어느 쪽을 쓸지 받음)는
  그대로 두고, 예/아니오 확인만 띄우는 초기화·설정에서는 뗐습니다.

## 0.8.0 — 2026-08-28

마우스 매크로가 녹음한 자리에 가도록 만든 릴리스입니다. 펌웨어를 다시 flash해야 합니다.

- **이동을 한 번에 쏘지 않고 녹음된 속도로 재생합니다.** 패드는 상대 좌표 마우스라
  "이만큼 움직여라"만 보내고, 화면에서 얼마나 가는지는 데스크톱의 포인터 가속이 정합니다.
  그런데 한 레코드는 50 ms짜리 이동 *조각*입니다 — 실제 마우스는 그동안 2 counts짜리
  리포트를 50번쯤 보냈는데 패드는 100 counts를 한 방에 보냈습니다. 가속 곡선은 그걸
  "매우 빠른 움직임"으로 읽고 증폭하고, 매크로는 오버슈트합니다. 빠르게 그린 제스처일수록
  더 어긋났습니다.

  녹음에는 그 동작이 얼마나 걸렸는지가 이미 들어 있습니다 — 뒤따르는 지연입니다. 이제
  펌웨어가 그 지연을 이동에 씁니다. 100 counts + 50 ms는 2 counts씩 50번으로 나가고,
  재생 속도가 녹음 속도와 같아져 가속 곡선이 상쇄됩니다. **레코드는 하나도 더 쓰지
  않습니다**(지연을 소비할 뿐입니다). 127을 넘어 쪼개진 조각은 지연을 나눠 쓰고, 사람이
  멈춰 있던 긴 지연은 150 ms에서 잘라 나머지는 그대로 지연으로 둡니다.

- **포인터 가속을 flat으로 바꿀지 물어봅니다.** flat이면 속도 변수 자체가 사라져 재생이
  정확해집니다. 마우스 녹음을 켤 때 감지해서 **승낙할 때만** 바꿉니다 — 남의 데스크톱
  설정이고 마우스 감각이 전부 바뀌는 일이라 조용히 건드리지 않습니다. 되돌리는 명령도
  같이 보여줍니다. "나중에"를 고르면 다시 묻지 않고, `Help > Mouse macro accuracy`가
  다시 물어보는 통로입니다. GNOME이 아니거나 설정을 읽을 수 없으면 아무 말도 하지
  않습니다(모름을 "이미 괜찮음"으로 읽지 않습니다).

- 알려진 채로 남는 것: pynput(X11) 폴백은 커서 절대 위치의 차이 — 즉 가속이 *이미
  적용된* 화면 픽셀 — 를 저장합니다. evdev는 raw 카운트고요. 같은 녹음이 백엔드에 따라
  다른 단위로 저장되며 X11에서는 재생 때 가속이 두 번 먹습니다. Wayland/evdev 경로는
  영향 없습니다.

## 0.7.0 — 2026-08-28

펌웨어를 다시 flash해야 합니다. 아래 세 가지 중 앞의 둘은 패드 안에서만 고쳐집니다.

- **`Reset…` 이후 패드가 옛 상태를 들고 있던 문제.** `RESET defaults=1`은 키맵을 새로 쓰는데,
  그 키맵을 이미 읽어 캐시해 둔 두 곳에 아무도 알려주지 않았습니다. 그래서 double 바인딩이
  방금 지워진 키가 여전히 탭을 250 ms 미루다가 더블탭에는 "빈 슬롯" 색으로 답했고, 픽셀은
  앱이 더 이상 보여주지 않는 밝기를 유지했습니다. 패드를 뽑았다 꽂기 전까지요 — 버튼이
  그렇게 하라고 말해주지도 않고요.
- **프로필을 쓴 뒤 밝기가 따라오지 않던 문제.** 같은 종류의 누락이 `PROF commit`에도 있었고,
  GUI는 밝기를 먼저 따로 보내기 때문에 가려져 있었습니다. `macrokey push`로 올리면 밝기만
  빼고 전부 반영됐습니다.
- **텍스트 런이 슬롯 끝을 넘으면 매크로가 뒤로 점프하던 문제.** 텍스트 헤더의 끝 위치를
  1바이트로 계산해서, 200번 레코드가 200자를 주장하면 268 → 12로 잘렸습니다. 잘림 검사를
  통과하고, 헤더보다 **앞으로** 돌아가 같은 구간을 영원히 반복합니다. 폭주 방지 데드라인도
  못 막습니다 — 문자마다 데드라인을 자기가 기다릴 시간만큼 밀어주기 때문입니다. 앱이 이런
  블롭을 만들지는 않지만, 증상이 "아무것도 타이핑하지 않고 아무 응답도 없는 패드"라
  가드가 가드 노릇을 하게 고쳤습니다.

- **연결이 끊기면 다시 붙습니다.** `PROTOCOL.md`가 처음부터 지수 백오프 재연결(1s → 2s → 4s
  → 최대 30s)을 규정했지만 구현이 없었습니다. 패드는 펌웨어를 올릴 때마다, 케이블을 건드릴
  때마다 다시 열거되고, 녹음을 시작·종료할 수 있는 유일한 수단이기도 합니다. 그래서 링크가
  끊기면 hold-to-record가 **조용히** 죽었습니다 — 3초를 눌러도 그냥 아무 일이 없었습니다.
  한 번이라도 맺어졌던 링크에만 재시도합니다. 앱을 켤 때 패드가 없었던 경우까지 계속 훑으면
  무관한 장치의 포트를 반복해서 여는 셈이고, 포트를 여는 것만으로 리셋되는 보드가 흔합니다.
  직접 Disconnect를 누른 경우도 재시도하지 않습니다.
- **녹음이 거절될 때 진짜 이유를 말합니다.** 전에는 무엇이 문제든 "too many steps or macros
  full. Shorten it"이었습니다. 볼륨 키가 하나 섞였거나 한글이 들어 있으면 길이와 무관하게
  거절되는데, 사용자는 짧게 만들라는 말만 듣고 원인을 못 찾았습니다. 이제 `the keypad cannot
  send 'volumeup'`, `the keypad types printable ASCII only…`, `the keypad's macro storage is
  full…`처럼 막은 스텝을 지목합니다.

- **죽은 코드 정리.** 도달할 수 없던 자동 반복(`KEYF_REPEAT`)을 걷어냈습니다. hold를 바인딩할
  수 없게 된 뒤로 tap·double이 모두 *릴리즈*에서 발화하므로, 이미 손을 뗀 키에 반복을
  무장했다가 다음 스캔에서 해제하기만 했습니다. 비트 번호는 wire format이라 예약해 둡니다.
  쓰이지 않던 `dirty_` 플래그도 없앴습니다.
- **문서와 구현 맞추기.** consumer 페이지 없는 빌드에서 `ACT_CONSUMER`가 "시리얼로 보고한다"는
  주석은 사실이 아니었고(아무것도 안 합니다), `ACT_LED_SCENE`의 페이로드는 "팔레트 인덱스"가
  아니라 예약 바이트입니다. `LED i=<0..7>`·`frame=…8개`는 픽셀이 1개인 지금 기준으로 고쳤습니다.

- **RP2040 기본작업.** 포팅은 아직 하지 않았고, 막고 있던 세 지점만 이음매로 정리했습니다.
  EEPROM 접근이 전부 `Storage.h`(`mkStoreBegin/Read/Update/Commit`)를 거치므로 플래시
  에뮬레이션 파트는 `MK_STORAGE_FLASH_EMULATED 1` 한 줄이면 됩니다. 부트로더 진입은 arch
  분기 뒤로 들어갔고(진입 방법이 없는 빌드는 `ERR code=unsupported`), 키 마스크는
  `mk_keymask_t` + `static_assert`가 되어 `MK_KEY_COUNT`를 8보다 크게 잡으면 조용히
  오동작하는 대신 빌드가 섭니다.
- **펌웨어 테스트가 시리얼 계층까지 내려갑니다.** PC 하니스에 `serial` 모드가 생겨서, 실제
  `SerialProtocol`에 명령을 먹이고 그 명령이 장치 상태에 무엇을 했는지 봅니다. 위의 RESET·
  commit 버그는 정확히 "`OK`를 답하고 낡은 상태를 들고 있는" 종류라 응답만 봐서는 못 잡습니다.

## 0.6.0 — 2026-08-27

- 단순 클릭을 좌상단으로 보내던 `mouse_home` 불일치를 제거했습니다. 기본 마우스 재생은 현재
  포인터 기준이며, 같은 화면 위치가 필요할 때만 실험 기능 `Fixed screen`을 켭니다.
- raw 마우스 이동을 50 ms 조각과 시간 간격으로 보존해 포인터 가속 때문에 이동 거리가 크게
  달라지는 문제를 줄였습니다. 고정 위치 홈 범위도 6096에서 16256 raw 단위로 늘렸습니다.
- 시리얼 reader/write 실패가 즉시 연결 상태를 내리고 대기 요청과 전역 녹화를 종료합니다.
- 프로필 불일치에서 Cancel하면 `Sync needed` 상태가 유지되고, 해결 전 로컬 편집은 패드를
  자동으로 덮어쓰지 않습니다. Pull/Push와 일반 프로필 쓰기는 UI 밖의 순차 worker에서 합니다.
- 직접 Clear/단축키 변경도 참조가 끊긴 매크로 저장소를 즉시 회수합니다.
- 툴바를 두 줄로 나누고 대비·접근성 라벨을 고쳤으며, 녹화 폐기·권한 설정 재시도·프로필
  가져오기/내보내기/직전 버전 복구·마우스 정확도 도움말을 추가했습니다.
- 독립 GUI 실행에도 회전 로그를 켜고, Auto 포트 선택 유지·generic clone 탐색 fallback·worker
  종료·EEPROM 장시간 쓰기 timeout·CI ruff 검사를 보강했습니다.

## 0.5.2 — 2026-08-26

툴바 레이아웃만 고쳤습니다. 펌웨어는 `0.5.0` 그대로입니다.

- **UI 폰트가 9 pt보다 크면 툴바 글자가 잘리던 문제.** `Reset…`이 잘린다는 제보로
  찾았는데, 버튼 하나가 아니라 **줄 전체가 창 최소 너비(하드코딩 820 px)에 들어가지
  않는** 문제였습니다. 툴바는 한 줄이고 줄바꿈을 하지 않아서, 좁아지면 무엇을 버리는 게
  아니라 위젯을 서로 겹쳐 밀어냅니다 — 포트 이름이 Connect 버튼 밑으로 들어가고 라벨이
  전부 잘립니다. 9 pt에서만 우연히 맞았습니다.
  - 버튼이 자기 라벨보다 좁아지지 않습니다. `Connect`는 가장 긴 상태값
    (`Connecting...`) 기준이라 상태가 바뀌어도 크기가 흔들리지 않습니다.
  - `Typing` 스핀박스의 고정 84 px을 없앴습니다. 가장 큰 값(`255 ms`) 기준으로 Qt가
    잽니다. 84 px은 `default`였을 때는 맞았고 `255 ms`는 11 pt부터 잘렸습니다.
  - 포트 콤보의 최소 너비도 폰트를 따라갑니다.
  - 창 최소 너비가 툴바 실제 폭을 따라갑니다. `_refresh_all()`이 라벨 텍스트를 채운
    **뒤에** 재야 합니다 — 그 전에 재면 60 px 모자랍니다.

## 0.5.1 — 2026-08-26

앱만 바뀌었습니다. 펌웨어는 `0.5.0` 그대로이고 다시 flash할 필요 없습니다.

- **툴바가 프로필과 다시 맞춰집니다.** `Brightness`와 `Typing`은 창을 만들 때 한 번만
  값을 읽었는데, 연결 시 "Profile differs"에서 **Pull**을 고르면 프로필 객체가 통째로
  교체되고 `_refresh_all()`은 이 둘을 갱신하지 않았습니다. 화면은 옛 값, 프로필은 패드 값인
  채로 영영 어긋났습니다. 새로 설치한 PC에서 특히 잘 걸립니다 — 설정 파일이 없어 기본값으로
  시작하니 불일치 대화상자를 반드시 거치기 때문입니다.
- **타이핑 속도에서 `default`라는 글자를 없앴습니다.** 이제 범위가 `1..255 ms`이고 패드
  기본값은 숫자 그대로 **5 ms**로 보입니다. 예전에는 0이 `default`로 표시되는데 위 화살표를
  한 칸 올리면 1 ms — 방금 떠난 기본값보다 **다섯 배 빠른** 값이 나왔고, 툴팁은 반대로
  말했습니다. 빠르게 치고 싶으면 1 ms로 내리면 됩니다.
  프로필에는 여전히 0으로 저장됩니다. 갓 flash한 패드의 헤더 바이트가 0이고 앱은 프로필을
  raw 바이트로 비교하므로, 5를 그대로 쓰면 새 설치마다 없는 차이로 "Profile differs"가
  뜹니다.
- **`Reset…` 버튼.** 툴바 오른쪽. **PC와 패드 양쪽의 바인딩과 녹음된 매크로를 전부 지우고**
  hyper + 1..8 기본값으로 되돌립니다. 패드는 push가 아니라 펌웨어 자신의 `RESET defaults=1`
  (`Profile::writeDefaults`)로 지웁니다 — 매크로 영역을 통째로 0으로 밀고, 양쪽이 우연이
  아니라 정의상 같아지는 지점이기 때문입니다. 패드가 거절하면 PC 프로필은 건드리지 않고
  멈춥니다. 되돌릴 수 없어서 확인창을 거칩니다.
- **`Include mouse` 기본값이 꺼짐입니다.** 대부분의 매크로는 키보드고, 조용히 켜져 있으면
  포인터가 스친 것까지 슬롯을 먹었습니다. 켜려면 에디터에서 체크하면 되고 설정은 유지되므로
  hold-to-record 전에 정해두면 됩니다. **이미 쓰던 `settings.json`은 그대로**라 기존 설치는
  바뀌지 않습니다.

## 0.5.0 — 2026-08-12

**Breaking: pad-only + GUI 배포.** 패드는 USB HID만, PC 앱은 설정·녹음·동기화 전용입니다.

- **제거:** `HOST` 런타임, clipboard/shell, `daemon`/`state`, AgentPet LED, `windows` extra.
- **녹음:** 장치에 안 들어가면 거절(호스트 fallback 없음). 저장 실패 시 기존 바인딩 유지.
  Wayland 첫 실행 시 `input` 권한 안내/`pkexec`.
- **연결:** 프로필 불일치 시 Pull / Push / Cancel.
- **펌웨어:** `ACT_HOST`/`EV t=host` 퇴역. 더블 슬롯 재녹음 시 기존 DOUBLE 억제.
  시퀀스 매크로 실행 중 시안 펄스 · 완료 시 초록 플래시.
- **배포:** `./build_release.sh` → `releases/linux/macrokey` (GUI onefile).
  `main.py` + `requirements.txt`. 배포 바이너리는 GUI만.
- **문서:** README 축소, `docs/manual.html`·`wiring.html`, ARCHITECTURE/HARDWARE 정리.
  프로필 **1024** B, flash는 `compile --upload`.

## 0.4.1 — 2026-08-09

- **타이핑 속도 설정.** 툴바의 `Typing`, 글자당 대기 시간(ms)이고 `default`는 5 ms입니다.
  연속 타자는 타이밍이 기록되지 않고 `text` 하나로 합쳐지므로 재생이 녹음보다 빠른데,
  받는 쪽이 따라오지 못할 때 쓰는 손잡이입니다. **프로필에 저장되므로 앱을 꺼도 패드가 그
  속도로 칩니다** — 헤더의 예약 바이트(11)에 들어가고, 0은 "펌웨어 기본값"이라 스키마 범프
  없이 기존 프로필과 호환됩니다. CRC는 본문만 덮으므로 영향 없습니다.
- **로그가 콘솔로 새지 않습니다.** 파일 핸들러를 붙이면서 루트 로거를 DEBUG로 올린 탓에
  녹음의 모든 스텝이 터미널로도 쏟아졌습니다. 레벨을 핸들러마다 두어, 파일은 전부 받고
  콘솔은 경고 이상만 받습니다(`--verbose`면 전부).

## 0.4.0 — 2026-08-09

패드에서 직접 녹음하는 것이 실제로 동작하게 만든 릴리스입니다. 펌웨어 · 호스트 앱 모두
`0.4.0`, 프로필 스키마 `2`, 시리얼 프로토콜 `v1`(호환).

**펌웨어와 앱을 함께 올려야 합니다.** 프로필 블롭은 크기가 같아서, 스키마 바이트가 없으면
옛 배치를 새 배치로 읽어 엉뚱한 값이 나옵니다. 옛 EEPROM은 펌웨어가 기본값으로 다시 씁니다.

### 녹음이 실제로 저장되지 않던 문제

`Action.to_dict`가 기본값과 비교하려고 같은 종류의 빈 인스턴스를 만들었는데, `text`는 비어
있으면 안 된다는 검증에 스스로 걸렸습니다. **타이핑이 들어간 녹음은 100% 저장 실패**했고,
상태 표시줄만 "could not write it to the keypad"라고 말한 뒤 조용히 사라졌습니다.

### 그 밖에 고친 것

- 같은 키에 다시 녹음하면 매크로 슬롯이 새로 소모되던 문제. 16번이면 슬롯이 다 차고 그
  뒤로는 전부 호스트 액션으로 떨어져, 앱을 끄면 패드가 동작하지 않게 됐습니다.
- 평범한 클릭이 드래그로 저장되던 문제. 누름과 뗌 사이에 이동이 조금이라도 있으면 드래그로
  봤는데, DPI 높은 마우스는 손 떨림만으로 수십 카운트라 사실상 모든 클릭이 드래그였습니다.
- 마우스 매크로가 엉뚱한 데를 클릭하던 문제. 상대 좌표라 재생 시작 위치에 따라 통째로
  어긋났습니다. 이제 녹음도 재생도 화면 좌상단에서 출발합니다.
- 녹음이 실제 입력을 버리던 문제. 패드 자신의 HID를 걸러내려던 150 ms 차단이, 패드 노드를
  제외한 뒤에도 남아 사용자의 타이핑만 버리고 있었습니다.
- 이미 매크로가 있는 키에 더블 녹음이 불가능하던 문제. 첫 탭이 그 매크로를 재생하고 그동안
  버튼 스캔이 멈춰, 두 번째 누름이 짝 판정 창을 지나 버렸습니다.
- 매크로 재생이 최대 30초 동안 펌웨어 전체를 멈추던 문제.
- 장치를 뽑아도 녹음이 끝나지 않던 문제. 이제 워치독이 폐기하고, 10분이 지나면 저장합니다.
- 자동 반복이 해제되지 않는 경로. 탭으로 무장되면 영원히 풀리지 않아, 호스트에서 보면
  키보드가 죽은 것과 구별되지 않습니다.
- 부팅 후 250 ms 안의 첫 누름이 짝의 두 번째로 오판되던 문제.

### 새로 생긴 것

- **패드에서 녹음.** 키를 단독 3초 홀드 → 녹음, 같은 키 다시 3초 → 저장. 탭 후 홀드는
  DOUBLE 슬롯(픽셀 분홍).
- **드래그 앤 드롭 녹음.** 누름과 뗌을 모두 캡처하고, 실제 이동량으로 클릭과 구분합니다.
- **`ACT_TEXT`.** 타이핑을 헤더 1개 + 문자 3개당 1레코드로 저장. 텍스트 기준 용량이 약
  273자에서 약 900자가 됐습니다.
- **`ACT_MOUSE_HOME`.** 커서를 좌상단으로 보내는 1레코드 액션.
- **항상 켜진 로그 파일** `~/.config/macrokey/macrokey.log`.
- **캡처 결과 표시.** 녹음이 저장되면 창에 실제로 돌아갈 스텝이 나열됩니다.
- **펌웨어↔호스트 정합성 테스트.** 진짜 펌웨어 소스를 PC용으로 컴파일해 호스트가 만든
  바이트를 먹입니다. 두 벌인 EEPROM 레이아웃이 어긋나면 잡힙니다.

### 없어진 것

- **레이어.** 이미 1개였고, 남은 건 언제나 0이던 `layer` 인자였습니다. momentary 상태 머신,
  `LAYER` 명령, `layer` 호스트 액션, 활성 창 폴링 스레드까지 함께 제거.
- **코드(chord).** 편집 수단이 없어 8슬롯 전부 영구히 비어 있었습니다.
- **hold 바인딩.** 녹음 진입점이라 예약됩니다.
- 액션 id 6 · 7은 재사용하지 않고 퇴역시켰습니다.

### 알아두실 것

**재생은 녹음보다 빠릅니다.** 연속 타자는 타이핑 속도를 기록하지 않고 `text` 하나로 합쳐져
글자당 5 ms로 재생됩니다 — 2.7초에 친 15글자가 0.075초입니다. 2초를 넘는 멈춤도 2초로
잘립니다. 받는 쪽에 준비 시간이 필요하면 녹음할 때 그만큼 더 기다렸다 다음 동작을 하세요.

**탭-홀드로 DOUBLE에 녹음할 때 첫 탭이 그 키의 TAP 매크로를 한 번 실행합니다.** 더블클릭에
첫 탭이 있는 이상 피할 수 없습니다.

## 2026-08-09 (5) — 마우스 매크로가 엉뚱한 데를 클릭하던 문제, 캡처 내용 표시

**증상: 녹음한 게 지맘대로 움직인다.**

커널은 마우스 이동을 **상대값(dx, dy)으로만** 알려줍니다. 그래서 녹음된 매크로를 재생하면
"커서가 지금 있는 곳"에서 그만큼 움직입니다. 녹음할 때 커서가 있던 자리와 재생할 때 있던
자리가 다르면 그 차이만큼 통째로 어긋나고, 매크로 안의 클릭은 전부 엉뚱한 걸 누릅니다.

상대 좌표를 고른 건 "창이 움직여도 살아남는다"는 이유였는데, UI를 클릭하는 매크로에는
틀린 선택이었습니다.

- **녹음이 시작될 때 패드가 커서를 좌상단 구석으로 보냅니다.** 그때부터 캡처되는 상대
  이동은 곧 원점 기준 절대 좌표가 됩니다. 커서를 옮기는 주체가 패드인 이유는 Wayland에서
  컴포지터 밖의 무엇도 커서를 옮길 수 없기 때문입니다 — 패드는 진짜 USB 마우스입니다.
- **마우스를 건드리는 장치 매크로는 `ACT_MOUSE_HOME`으로 시작합니다.** 재생도 같은 구석에서
  출발하므로 녹음할 때와 같은 픽셀에 떨어집니다. 펌웨어가 슬램 루프를 도는 덕분에 레코드
  1개입니다(호스트가 이동으로 내보냈으면 48개).
- 키보드만 있는 매크로에는 붙지 않습니다. 누를 때마다 커서가 구석으로 튀면 그것도 버그입니다.

**캡처된 스텝을 앱에서 볼 수 있습니다.** 녹음은 화면 없이 만들어집니다 — 패드에 화면이 없고
창은 열려 있지 않아도 됩니다. "내가 한 것과 다르다"를 확인할 방법이 없어서 추측밖에 할 수
없었습니다. 이제 녹음이 저장되면 창에 스텝이 그대로 나열되고, 로그에도 남습니다.

## 2026-08-09 (4) — 이미 매크로가 있는 키에는 더블 녹음이 불가능했던 문제

**증상: 한 번 녹음한 뒤 다시 녹음하면 덮어쓰이지 않는다.**

더블 슬롯에 녹음하는 동작은 "탭 → 250 ms 안에 다시 눌러 3초 홀드"입니다. 그런데 그 **첫
탭이 키에 걸린 것을 발동시킵니다.** 그게 매크로면 `runMacro`가 `loop()`을 막고, 버튼
스캔도 같이 멈춥니다. 두 번째 누름은 매크로가 끝난 뒤에야 감지되므로 타임스탬프가 몇 초
늦고, 짝 판정 창(250 ms)을 한참 지나 **그냥 홀드로 읽힙니다.**

결과: 이미 매크로가 있는 키 — 즉 다시 녹음할 이유가 있는 모든 키 — 에서는 더블 슬롯 녹음이
불가능했고, 대신 tap 슬롯이 또 덮어써졌습니다.

- `KeyEngine::macroPump()`을 추가해 매크로 재생 중에도 **버튼 스캔을 계속 돌립니다.**
  지연 구간뿐 아니라 레코드마다 한 번씩 — 드래그처럼 지연이 없는 매크로는 그러지 않으면
  한 번도 양보하지 않습니다. 시리얼은 여전히 일부러 큐에 둡니다(EEPROM 안전).
- 스캔 펌핑을 스케치가 아니라 엔진에 뒀습니다. 막고 있는 주체가 엔진이고, 그래야 하네스로
  검증됩니다.

**더블 매크로를 녹음해도 발동하지 않던 문제도 함께 고쳤습니다.** 어느 키에 더블
바인딩이 있는지는 엔진이 캐시하는 유일한 값인데(`doubleTapMask_`), 프로필이 새로 써져도
갱신되지 않았습니다. 갱신은 `begin()`에서 한 번뿐이라, 방금 녹음한 더블 매크로는 패드를
뽑았다 꽂기 전까지 250 ms 지연 대상에 들어가지 못했고 따라서 절대 발동하지 않았습니다.
`PROF commit` 성공 시 `noteProfileChanged()`로 무효화합니다.

하네스에 회귀 테스트 2개 추가: 더블 슬롯이 이미 찬 키에서의 탭-홀드 판정, 그리고 매크로
재생 중 버튼 스캔이 도는지.

## 2026-08-09 (3) — 레이어 제거, 펌웨어 정합성 테스트, 키보드 멈춤 원인

### 실제 키보드가 가끔 멈추는 문제

두 가지를 찾았습니다.

- **레코더가 패드 자신의 키보드 노드를 읽고 있었습니다.** 패드는 USB 키보드이자 마우스라
  `/dev/input/event*`에 그대로 나타나고, 레코더는 그걸 다른 키보드처럼 열었습니다. 매크로가
  제 꼬리를 먹는 걸 막으려고 **패드 입력 후 150 ms 동안 모든 입력을 버리는** 우회책이 있었고,
  그 사이에 실제로 치고 있던 것도 같이 버려졌습니다. 이제 USB 벤더로 패드 노드를 골라내
  아예 열지 않습니다 — 우회책도 데이터 손실도 없어집니다.
- **`repeatKey_`가 해제되지 않는 경로가 있었습니다.** 해제는 `released=true` 이벤트에서만
  일어나는데 그건 HOLD에서만 나옵니다. TAP은 `released=false`로 올라오고 억제된 키는 아무
  이벤트도 내지 않으므로, 탭으로 무장된 auto-repeat는 **영원히** 풀리지 않고 패드가 초당
  8번 같은 키를 계속 보냅니다. 호스트에서 보면 키보드가 죽은 것과 구별되지 않습니다.
  지금은 물리 상태(`pressedMask()`)로 해제합니다. 현재 `repeat` 플래그를 세우는 경로는
  없어서 잠복 상태였지만, 증상이 정확히 이것입니다.

재생이 키나 버튼을 누른 채 끝내지 않는지 검사하는 회귀 테스트를 추가했습니다.

### 펌웨어↔호스트 정합성 테스트를 상설화

`firmware/test/`에 Arduino 스텁 헤더와 하네스를 넣고, `tests/test_firmware_agreement.py`가
**진짜 펌웨어 소스를 PC용으로 컴파일해서** 호스트 인코더가 만든 바이트를 먹입니다. 레이아웃
상수, 키맵, 매크로 레코드, 홀드 제스처 판정, 재생 HID 호출까지 대조합니다. C++ 컴파일러가
없으면 실패가 아니라 skip입니다. 호스트 쪽 상수를 하나 어긋나게 해서 실제로 잡히는지도
확인했습니다.

### 레이어 제거

`LAYER_COUNT`는 이미 1이었지만 `layer` 인자가 거의 모든 호출을 관통하며 언제나 0이었습니다.

- `Profile.layers[0].keys[key]` → `Profile.keys[key]`. `action(key, gesture)`,
  `set_action(key, gesture, ...)`.
- momentary 레이어 상태 머신, `LAYER` 시리얼 명령, `layer` 호스트 액션, 활성 창을 1초마다
  폴링하던 앱별 레이어 스레드, 키별 색(픽셀은 하나입니다) 삭제.
- 액션 id 6·7은 **재사용하지 않고 퇴역**시켰습니다. id는 wire format이라 위쪽을 밀면 저장된
  매크로가 전부 다른 매크로가 됩니다.
- **EEPROM 바이트는 그대로입니다.** 스키마 범프도, 재플래시로 인한 녹음 손실도 없습니다.
- 펌웨어가 오히려 작아졌습니다: 플래시 19908 → 19556 B, 전역 1068 → 1049 B.

예전 모양(`layers` 배열)으로 저장된 프로필도 계속 읽힙니다 — 내용이 있던 레이어는 어차피
하나뿐이었습니다.

### ui/app.py 분리

1038줄 한 파일이 `app.py`(창) · `slot_dialog.py` · `widgets.py` · `describe.py`로 갈렸습니다.

## 2026-08-09 (2) — 더블클릭 후 홀드로 double 슬롯에 녹음

패드만으로 도달할 수 있는 슬롯이 tap 하나뿐이었습니다. `_finish()`가 `"tap"`으로
하드코딩되어 있어서, 키의 두 번째 슬롯은 편집 창을 열어야만 채울 수 있었습니다 — 키를
눌러서 프로그래밍한다는 게 전제인 장치에서.

게다가 펌웨어는 이미 더블탭 후 홀드를 녹음 경로로 통과시키고 있었습니다. `recordFired`는
*짝의 두 번째가 아닌* 프레스 엣지에서만 리셋되기 때문입니다. 그래서 탭-탭-홀드는 레코더를
열었고, 결과는 아무 말 없이 tap 슬롯에 저장됐습니다.

- **홀드 3초 → tap, 탭 후 홀드 3초 → double.** `EV t=record`가 `g=`를 함께 보냅니다.
- 짝 판정은 **릴리즈와 다음 프레스 사이의 간격만** 봅니다. 탭 지연(`doubleTapMask_`)에
  얹지 않은 이유는 그 마스크가 *이미 더블 바인딩이 있는 키*에만 켜지기 때문입니다 —
  녹음이 필요한 건 정확히 더블 슬롯이 비어 있는 키라서 닭과 달걀이 됩니다. 지연 비용도
  없습니다: 바인딩 없는 키는 여전히 떼는 즉시 탭이 나갑니다.
- **어느 슬롯인지는 시작할 때 확정됩니다.** 끝낼 때는 같은 키이기만 하면 됩니다. 끝내는
  쪽이 슬롯을 정하면, tap 녹음을 탭-탭-홀드로 끝냈을 때 조용히 다른 슬롯으로 옮겨가고
  픽셀은 녹음 내내 거짓말을 한 셈이 됩니다.
- **픽셀이 어느 슬롯인지 말합니다**: tap은 빨강, double은 분홍. 화면이 없으니 녹음이
  도는 동안 그걸 말할 자리가 거기밖에 없습니다.
- 구형 펌웨어(`g` 없음)는 `tap`으로 읽고, 알 수 없는 값도 `tap`으로 떨어집니다 —
  `hold` 같은 값이 `set_action`에 닿으면 거부당하면서 방금 만든 녹음을 잃게 됩니다.

**부팅 직후 오판 버그 하나를 같이 잡았습니다.** `releasedAt`이 0으로 시작해서 "uptime 0에
떼졌다"로 읽혔고, 부팅 후 `MK_DOUBLE_TAP_MS` 안의 첫 프레스는 짝의 두 번째로 보였습니다.
거기서 그냥 홀드하면 double 슬롯이 프로그래밍됐습니다. 실제 `ButtonInput`을 프레스 패턴에
태워 보다가 드러났습니다.

## 2026-08-09 — 녹음 용량 3배, 그리고 녹음을 갉아먹던 버그들

**프로필 schema가 1 → 2로 올라갑니다.** 블롭 크기는 그대로 1024바이트라 스키마 바이트가
없으면 옛 배치를 새 배치로 읽어 엉뚱한 값이 나옵니다. **펌웨어와 앱을 함께 갱신해야
합니다.** 옛 EEPROM은 펌웨어가 기본값으로 다시 씁니다(기존 녹음은 사라집니다).

### 고친 것

- **같은 키에 다시 녹음하면 매크로 슬롯이 새로 소모되던 문제.** 이전 슬롯을 회수하는
  코드가 없어서, 키 하나를 16번 고쳐 녹음하면 슬롯 16개가 전부 도달 불가능한 스텝으로
  차고 그 뒤로는 모든 녹음이 조용히 호스트 액션으로 떨어졌습니다 — 즉 앱을 끄면 패드가
  동작하지 않게 됩니다. 호스트 액션 토큰도 같은 방식으로 누수됐습니다. 이제
  `assign_recording`이 대상 키의 바인딩을 먼저 비우고 참조되지 않는 저장소를 쓸어냅니다.
  프로필을 읽을 때도 한 번 쓸어내므로 이미 망가진 프로필은 다음 실행에서 복구됩니다.
- **CLI가 `--gesture hold`를 받아주던 문제.** hold는 녹음을 여는 제스처인데 바인딩이
  세션 안에서는 살아남아 그대로 장치에 기록됐습니다. `Profile.set_action`이 이제 예약된
  제스처에 대한 쓰기를 거부합니다. `--layer`는 레이어가 하나뿐이라 삭제했습니다.
- **매크로 재생 중 펌웨어 전체가 멈추던 문제.** `runMacro`는 `loop()` 안에서 도는데 상한이
  30초였습니다. 그동안 버튼·시리얼·LED가 전부 정지했고 호스트는 링크가 죽은 줄 압니다.
  상한을 10초로 되돌리고, 지연 중에 LED를 계속 갱신합니다(시리얼은 일부러 큐에 둡니다 —
  매크로가 EEPROM을 읽는 중에 프로필 쓰기가 커밋되면 스텝이 발밑에서 바뀝니다).
- **장치를 뽑아도 녹음이 계속되던 문제.** 녹음을 끝낼 수 있는 건 패드뿐인데 `abort()`를
  호출하는 곳이 없었습니다. 세션이 자체 워치독 스레드를 갖고, 연결이 끊기면 녹음을
  폐기하고 10분이 지나면 저장한 뒤 멈춥니다.
- **UI 스레드에서 블로킹 시리얼 I/O.** LED 킵얼라이브가 `QTimer`로 메인 스레드에서 돌아,
  패드가 응답을 멈추면 5초마다 최대 4초씩 창이 굳었습니다. 워치독 스레드로 옮겼습니다.
- **드래그 앤 드롭이 녹음되지 않던 문제.** 버튼 릴리즈를 버리고 모든 버튼을 완전한 클릭
  하나로 만들었기 때문에, 드래그가 "클릭 → 순간이동 → 클릭"으로 재생됐습니다. 이제 누름과
  뗌을 모두 캡처하고, 사이에 아무 일도 없었으면 클릭으로 합칩니다.
- **포인터 이동 분할이 직선이 아니던 문제.** 축마다 따로 ±127로 자르면 경로가 꺾입니다
  (300,-200 → 마지막 구간이 수평). 이제 두 축을 비례 배분합니다.
- 3초 홀드 중 400 ms 지점에서 "바인딩 없음" 빨간 플래시가 뜨던 문제. 녹음하려고 누르는
  중에 거부 신호가 나오는 셈이었습니다. hold는 이제 "등록됨, 계속 누르세요" 큐만 냅니다.
- `ButtonInput::begin`의 초기화 목록이 멤버 수보다 짧던 것, 의미 없던 `static_assert`,
  `normalize`의 변수 섀도잉, pynput 없이 실패하던 테스트 2개.

### 녹음 용량: 273 → 308 레코드, 텍스트 기준 약 273자 → 약 900자

- **`ACT_TEXT` 도입.** 타이핑한 텍스트를 헤더 레코드 1개 + 문자 3개당 1개로 저장합니다.
  이전에는 문자마다 3바이트 `ACT_KEY`였고, `sudo apt update && sudo apt upgrade -y` 한
  줄이 38 스텝(전체 273개 중)이었습니다. 지금은 14 레코드입니다.
- **키맵에서 hold 칸 제거** (32 B). 바인딩할 수 없는 제스처라 모든 키에서 0이었습니다.
- **코드(chord) 영역 제거** (40 B). 에디터가 편집 수단을 준 적이 없고 기본값도 비어 있어
  8개 슬롯 전부가 영구히 0이었습니다.
- **매크로 인덱스에서 오프셋 제거** (32 B). 슬롯은 순서대로 채워지므로 시작 위치는 앞
  슬롯들의 레코드 수 합입니다. 인덱스와 영역이 매크로 시작점을 두고 서로 다른 말을 하는
  종류의 버그도 함께 사라집니다.
- 프로필이 EEPROM 1024바이트를 남김없이 씁니다.

### 검증

- 파이썬 테스트 244개 통과 (PySide6 없으면 223, pynput도 없으면 221 + 4 skip).
- `arduino-cli`로 실제 보드 빌드 (`--warnings all`, 경고 0):
  SparkFun Pro Micro · Arduino Leonardo · Arduino Micro.
  **플래시 19830 B / 28672 B (69%), 전역 변수 1051 B / 2560 B (41%).**
  전역 변수는 변경 전(1053 B)보다 오히려 2 B 줄었습니다 — 코드 영역 제거분이
  `ACT_TEXT`·드래그 추가분을 상쇄합니다.
- **SRAM 여유 실측.** `-fstack-usage`(LTO off)로 스택 프레임을 재서, 프로필 쓰기 중
  1024 B 스테이징 버퍼를 든 상태의 최악 경로가 약 200 B 여유로 들어감을 확인했습니다.
  계산 근거는 `Profile.h`의 스테이징 주석에 남겼습니다 — AVR은 MMU가 없어 스택이 넘치면
  조용히 전역을 망가뜨리므로, 다음에 버퍼를 키우는 사람이 다시 재도록.
- **호스트↔펌웨어 정합성.** 호스트 인코더가 만든 블롭을 펌웨어 자신의 `Profile` 리더에
  통과시켜 키맵·매크로 레코드가 바이트 단위로 일치함을 확인.
- **재생 검증.** 같은 블롭을 실제 `KeyEngine::runMacro`에 태워 나오는 HID 호출을 확인:
  텍스트 런이 문자 단위로 타이핑되고, 드래그가 `press → 등간격 이동 3회 → release`로
  재생되며, 재생 종료 시 모든 버튼·키가 해제됩니다.

## 2026-08-04 (2) — GUI를 PySide6로 교체

- **편집기 창을 Tkinter에서 PySide6로 옮겼습니다.** `ui/app.py` 한 파일만 다시 썼고
  `config/` · `device/` · `actions/` · `recorder/` · `led/` · `backends/`는 한 줄도
  건드리지 않았습니다. "`ui/`를 제외한 어느 모듈도 UI 툴킷을 import 하지 않는다"는 규칙이
  실제로 값을 한 지점입니다.
- 화면 구성과 동작은 그대로입니다. 툴바(Port · Connect · Disconnect · Save · Write to device ·
  Read from device · Brightness), 레이어 탭 4개, 키 8행 × 제스처 3열 = 96슬롯 격자, 상태 표시줄,
  슬롯 편집·녹화 다이얼로그, 값 검증 시점(OK 누를 때) 모두 동일합니다.
- 스레드→UI 전달 방식이 `root.after(0, ...)`에서 **Qt 시그널**로 바뀌었습니다
  (`statusMessage` · `failed` · `pulled`). 장치 통신은 이전과 같이 평범한 스레드에서 돕니다.
- **`PySide6`를 선택 의존성 `gui`로 추가했습니다.** Qt는 용량이 크고 헤드리스 사용 경로가
  실재하므로 필수로 넣지 않았습니다. 없으면 `macrokey gui`만 설치 안내(`MissingToolkit`)와
  함께 종료 코드 2로 끝나고 나머지 명령은 그대로 동작합니다.
  설치는 `pip install -e ".[gui,input]"`입니다.
- `cli.py`가 `from .ui.app import run_gui` 대신 `from .ui import run_gui`를 씁니다.
  안내 메시지를 담은 얇은 진입점을 거치게 하기 위해서입니다.

## 2026-08-04

저장소 레이아웃을 평탄화하고 사용 문서를 최상위로 올렸습니다. 코드 동작은 그대로입니다.

- **`arduino/macrokey/` → `firmware/`**, 스케치 이름도 `macrokey.ino` → `firmware.ino`.
  아두이노는 스케치 파일 이름이 폴더 이름과 같아야 하므로 함께 바뀝니다.
  빌드는 이제 `arduino-cli compile ... firmware`입니다.
- **`host/macrokey/` → `macrokey/`**, `host/pyproject.toml` → `pyproject.toml`.
  중간 `host/` 계층이 없어져 파이썬 앱 폴더로 바로 들어갑니다.
  설치는 `pip install -e ".[input]"`입니다.
- **`docs/manual.html` → `manual.html`, `docs/wiring.html` → `wiring.html`.**
  가장 자주 여는 두 문서를 최상위로 올리고 내용을 요약했습니다
  (사용 설명서 1042 → 375줄, 배선 가이드 1070 → 504줄). 핀 맵과 전체 결선도 그림은
  그대로 두고 설명 문장, 화면 목업, 레시피 모음, 중복된 전력 예산 절을 덜어냈습니다.
  전력 계산 근거는 `docs/HARDWARE.md`에 그대로 있습니다.
- **`host/macros/*.png` 샘플 이미지 8개를 삭제했습니다.** 구버전 "단축키 → 이미지 붙여넣기"
  앱의 잔재이고 코드가 참조하지 않았습니다. `clipboard_image` 액션과 구버전
  `bindings.json` 마이그레이션은 그대로 남아 있습니다.
- 위 삭제에 따라 `resolve_asset()`의 상대 경로 기준을 저장소 경로에서 **설정 폴더**
  (`profile.json`이 있는 곳)로 바꿨습니다. 기존 기준 폴더에는 이제 참조할 자산이 없고,
  설치본에서는 애초에 의미가 없는 경로였습니다. 절대 경로와 `~`는 이전과 동일합니다.

## 2026-08-03

저장소를 `arduino/` · `host/` · `docs/` 세 트리로 분리하고, 펌웨어와 호스트 앱을 모두
재작성했습니다. 펌웨어 및 호스트 앱 버전 `0.3.0`, 시리얼 프로토콜 `v1`.

### 펌웨어 (`arduino/macrokey/`)

- 단일 스케치 `05_macrokey.ino`를 모듈 구조로 재작성했습니다.
  `KeyEngine` · `Profile`(EEPROM) · `LedController` · `LedEffects` · `ButtonInput` ·
  `SerialProtocol` · `HidBackend` · `ActionTypes` · `Config` · `Util`.
- `TAP` / `DOUBLE` / `HOLD` 제스처와 레이어 4개를 추가해 키 8개를 96 슬롯으로 확장했습니다.
  동시 입력 코드(chord) 테이블 8개를 함께 넣었습니다.
- 액션 타입 12종을 도입했습니다 (`KEY`, `CONSUMER`, `MOUSE_*`, `LAYER_*`, `SEQUENCE`,
  `HOST`, `LED_SCENE`, `DELAY`). 액션은 4바이트 고정 폭이고 키맵은 RAM 미러링 없이
  EEPROM에서 직접 읽습니다.
- EEPROM 프로필 저장소(헤더 + 키맵 + 코드 + 팔레트 + 장치 매크로, CRC16 포함, 1016 B)를
  추가했습니다. 매크로를 바꿀 때 펌웨어를 다시 빌드하지 않습니다.
- WS2812 8픽셀 지원(D11)을 추가했습니다. 우선순위 합성, 50 Hz 상한, 변경 시에만 송출,
  400 mA 전류 상한 스케일링, 기본 밝기 25%.
- 호스트 LED 소유권에 3초 워치독을 넣어 앱 종료 시 마지막 색으로 얼어붙지 않게 했습니다.
- 부팅 후 2초 HID 유예 시간을 추가했습니다.

### 호스트 앱 (`host/macrokey/`)

- 단일 파일 Tkinter 앱(`python_app/macro_key_app.py`)을 `macrokey` 패키지로 재작성했습니다.
  `config/` · `device/` · `actions/` · `recorder/` · `led/` · `backends/` · `ui/`.
- CLI를 추가했습니다: `gui`, `ports`, `info`, `push`, `pull`, `monitor`, `daemon`,
  `record`, `state`. 모든 명령이 GUI 없이 동작합니다.
- 시리얼 클라이언트(포트 자동 탐색, 자동 재연결, 프로토콜 v1 코덱)를 추가했습니다.
- 매크로 레코더를 추가했습니다. modifier 접기, 지연 양자화, 연속 문자 병합, 자기 입력 제외를
  거쳐 장치 액션 또는 호스트 액션으로 자동 배치합니다.
- 호스트 액션 registry를 추가했습니다 (`hotkey`, `text`, `clipboard_image`, `shell`,
  `sequence`, `delay`, `layer`, `noop`). 새 액션은 `@register` 한 줄로 늘어납니다.
- LED 서비스와 AgentPet 이벤트 프로토콜 v1 소스를 추가했습니다. `state`/`severity`/
  `progress`를 색·효과·진행률 바로 매핑합니다.
- 플랫폼 백엔드(클립보드·활성 창·키보드)를 런타임 선택으로 분리했습니다. 기존 앱이
  `win32clipboard`를 최상위에서 import 해 Windows 밖에서 이미지 기능이 통째로 죽던 문제를
  해결했습니다. 이제 없는 기능은 해당 액션 하나만 실패합니다.
- `ui/`를 제외한 모든 모듈이 tkinter를 import 하지 않습니다. 헤드리스 실행이 가능합니다.
- 프로필/설정을 플랫폼별 표준 설정 폴더에 저장하고, 구버전 `bindings.json`을 한 번
  자동 마이그레이션합니다. `MACROKEY_CONFIG_DIR`로 위치를 바꿀 수 있습니다.
- `pyproject.toml`로 패키징했습니다. `pyserial`만 필수이고 `input`(pynput),
  `windows`(Pillow, pywin32), `dev`는 선택 의존성입니다.

### 문서

- `docs/ARCHITECTURE.md` — 설계 원칙, 계층 구조, 입력·액션 모델, 메모리 예산,
  LED 파이프라인, 확장 지점, 의도적으로 하지 않은 것.
- `docs/HARDWARE.md` — 핀 맵, 배선 주의사항, 전력 예산, WS2812와 USB 타이밍, 빌드/업로드.
- `docs/PROTOCOL.md` — 시리얼 프로토콜 v1 메시지 규격.
- `docs/manual.html` — 사용 설명서. 처음 연결, 제스처·레이어 조작 모델, 기본 프로필,
  GUI 키 설정 절차, 액션 종류와 단축키 문법, 매크로 녹화, 호스트 액션 JSON 작성법,
  CLI 명령, LED 상태 매핑, 파일 위치, 레시피 6개, 그리고 **아직 안 되는 것** 목록.
- `docs/wiring.html` — 그림으로 된 배선 가이드. Leonardo 핀 맵, 버튼 1개 상세 회로,
  WS2812B 결선(330 Ω · 커패시터 위치 포함), 전체 결선도, 전력 예산 그래프, 증상별 원인 표,
  부품 목록. 라이트/다크 테마와 A4 인쇄를 모두 지원합니다.
### 하드웨어 확정 — Pro Micro · WS2812B 1개

실제 부품이 Pro Micro와 WS2812B 1개로 정해져서 펌웨어·호스트·문서를 전부 맞췄습니다.

- **보드를 Pro Micro(5 V / 16 MHz)로 확정.** `MK_BOARD_NAME`을 `promicro`로 바꿨습니다.
- **LED 데이터 핀을 D11 → A0(디지털 18)로 변경.** Pro Micro는 D11·D12·D13을 아예 밖으로
  빼지 않아서 기존 설정으로는 배선이 불가능했습니다. 노출된 핀 중 D2/D3은 SDA/SCL,
  D14/D15/D16은 SPI라 어느 버스와도 겹치지 않는 A0를 골랐습니다. VCC·GND와 같은 쪽 헤더라
  배선도 한쪽으로 정리됩니다. 버튼(D3–D10)은 Pro Micro에도 전부 있어 그대로입니다.
- **`MK_LED_COUNT`를 8 → 1로,** 호스트 `LED_COUNT`도 함께 1로 내렸습니다. EEPROM 팔레트가
  96 B에서 12 B로 줄어 프로필 전체가 1016 B → 932 B가 됐습니다.
- **버그 수정: 픽셀보다 키가 많을 때 대부분의 키가 LED 피드백을 잃던 문제.**
  `LedController::notePress`가 `key < MK_LED_COUNT`일 때만 반응해서, 1픽셀에서는 1번 키를
  누를 때만 LED가 반짝였습니다. 이제 남는 키는 마지막 픽셀로 접습니다.
- **버그 수정: 팔레트 직렬화가 인접 영역을 덮어쓰던 문제.** `binary.py`가 팔레트 주소를
  픽셀 수로 계산하면서 키 수만큼 루프를 돌아, `LED_COUNT < KEY_COUNT`이면 다음 레이어와
  매크로 영역까지 침범했습니다. 이제 `key_index < LED_COUNT`인 슬롯만 씁니다.
- `MK_LED_MAX_MILLIAMPS`를 400 → 100으로 조정했습니다. 1픽셀은 60 mA를 넘길 수 없어 실제로는
  걸리지 않지만, `MK_LED_COUNT`를 올리면 바로 안전장치로 동작하도록 남겨 뒀습니다.
- 커패시터 권장값을 220 µF → **100 µF**로 낮췄습니다(픽셀 1개 기준).
- 빌드 절차를 SparkFun 보드 패키지 기준으로 바꿨습니다
  (`SparkFun:avr:promicro:cpu=16MHzatmega32U4`). Pro Micro는 리셋 버튼이 없어 `RST`–`GND`를
  빠르게 두 번 단락시켜 부트로더를 띄워야 한다는 안내를 추가했습니다.
- 문서에 **픽셀 1개의 표현력 한계**를 명시했습니다. 키별 위치 표시는 불가능하고,
  `progress`는 진행률 바가 아니라 밝기로 나타납니다. LED의 주된 역할은 레이어 표시입니다.
- `docs/wiring.html`의 그림 1·3·4를 Pro Micro와 단일 픽셀 기준으로 다시 그리고,
  그림 5(전력)를 "USB 예산 대비 실제 소비"로 바꿨습니다.

### 전원 전제

- 전원 전제를 **PC USB 단독 급전**으로 명시하고 문서 전체를 여기에 맞췄습니다.
  - WS2812B 커패시터 권장값을 1000 µF에서 **220 µF**(100–470 µF)로 낮췄습니다. 1000 µF는
    전용 어댑터 기준값이고, USB 2.0은 장치 벌크 커패시턴스를 10 µF로 제한하기 때문에
    돌입 전류로 포트 보호가 걸릴 수 있습니다.
  - 포트 종류별 가용 전류표(USB 2.0 / 3.x / 무전원 허브 / 열거 전)와 케이블 전압 강하
    주의를 추가했습니다.
  - 외부 5 V 전원 절은 "지금은 필요 없음, 픽셀 16개 이상으로 늘릴 때의 메모"로 격하했습니다.
  - `Config.h`의 `MK_LED_MAX_MILLIAMPS` 주석에 400 mA 근거(`LED 400 + MCU 30 = 430`)와
    무전원 허브에서 250으로 낮추라는 안내를 적었습니다.
- README를 새 구조에 맞춰 다시 썼습니다.

## 2026-05-30

- Merged the Arduino keypad and Python macro helper into one `macroKey` project.
- Added a Tkinter Python app for editable hotkey-to-image bindings.
- Added README documentation for the Arduino HID macro keypad lab.
- Added license, contribution guidance, and security policy.
- Replaced the inline scan interval literal with `SCAN_INTERVAL_MS`.
